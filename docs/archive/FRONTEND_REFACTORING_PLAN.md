# План рефакторинга фронтенда

**Дата:** 2026-01-21  
**Статус:** План составлен

---

## 🎯 Цель

Устранить дублирование кода во фронтенде после рефакторинга бекенда, унифицировать работу с API, улучшить поддерживаемость и типобезопасность.

---

## 📊 Анализ проблем

### 🔴 Критичные проблемы

1. **Дублирование fetch вызовов**
   - Некоторые компоненты используют `fetchWithErrorHandling`, некоторые обычный `fetch`
   - Отсутствует единый подход к обработке ошибок
   - **Затронутые файлы:**
     - `ChatHistory.tsx` - использует обычный `fetch` без обработки ошибок
     - `MetricsDashboard.tsx` - использует обычный `fetch`
     - `MessageList.tsx` - использует обычный `fetch`
     - `modelUtils.ts` - использует обычный `fetch`

2. **Дублирование логики определения dev режима**
   - Логика `isDev` повторяется в нескольких местах
   - **Затронутые файлы:**
     - `useAgentStream.ts` (строка 290)
     - `MetricsDashboard.tsx` (строка 162)

3. **Отсутствие централизованного API клиента**
   - Каждый компонент делает fetch самостоятельно
   - Нет единой точки для управления API запросами
   - Сложно отслеживать и изменять API endpoints

### 🟡 Средние проблемы

4. **Дублирование типов**
   - `ValidationResult` определен в `useAgentStream.ts` и `useCodeExecution.ts`
   - `IndexProjectRequest` и `IndexProjectResponse` дублируются в `types/api.ts` (строки 97-109 и 206-218)

5. **Дублирование логики загрузки моделей**
   - `App.tsx`, `SidebarOptions.tsx`, `modelUtils.ts` - все загружают модели
   - Разная обработка ошибок

6. **Дублирование обработки состояния загрузки**
   - Множество компонентов имеют одинаковую логику `loading`, `error`, `data`
   - Можно вынести в кастомные хуки

### 🟢 Низкие проблемы

7. **Дублирование констант**
   - Проверить наличие дублирующихся констант в компонентах

8. **Дублирование логики localStorage**
   - Несколько компонентов работают с localStorage одинаково

---

## 📋 Пошаговый план рефакторинга

### Фаза 1: Унификация работы с API (КРИТИЧНО)

#### Шаг 1.1: Создать централизованный API клиент
**Время:** 2-3 часа  
**Файлы:**
- ✅ Создать `frontend/src/services/apiClient.ts`
- ✅ Вынести логику определения dev режима
- ✅ Создать унифицированные методы для всех API endpoints

**Критерии готовности:**
- Все API вызовы используют единый клиент
- Единая обработка ошибок
- Единая логика определения dev/prod режима

---

#### Шаг 1.2: Мигрировать все компоненты на API клиент
**Время:** 3-4 часа  
**Файлы:**
- ✅ `ChatHistory.tsx`
- ✅ `MetricsDashboard.tsx`
- ✅ `MessageList.tsx`
- ✅ `modelUtils.ts`
- ✅ `App.tsx`
- ✅ `SidebarOptions.tsx`
- ✅ `IDEPanel.tsx`
- ✅ `useCodeExecution.ts`

**Критерии готовности:**
- Все компоненты используют `apiClient`
- Нет прямых вызовов `fetch`
- Единая обработка ошибок везде

---

### Фаза 2: Унификация типов и интерфейсов

#### Шаг 2.1: Устранить дублирование типов
**Время:** 1-2 часа  
**Файлы:**
- ✅ Удалить дублирующиеся `ValidationResult` из `useAgentStream.ts` и `useCodeExecution.ts`
- ✅ Удалить дублирующиеся `IndexProjectRequest/Response` из `types/api.ts`
- ✅ Централизовать все типы в `types/api.ts`

**Критерии готовности:**
- Нет дублирующихся типов
- Все типы в одном месте
- Импорты обновлены

---

#### Шаг 2.2: Создать кастомные хуки для общих паттернов
**Время:** 2-3 часа  
**Файлы:**
- ✅ Создать `hooks/useApi.ts` - универсальный хук для API запросов
- ✅ Создать `hooks/useModels.ts` - хук для работы с моделями
- ✅ Создать `hooks/useLocalStorage.ts` - хук для работы с localStorage

**Критерии готовности:**
- Компоненты используют кастомные хуки
- Устранено дублирование логики состояния

---

### Фаза 3: Оптимизация компонентов

#### Шаг 3.1: Рефакторинг компонентов с дублированием
**Время:** 2-3 часа  
**Файлы:**
- ✅ Проверить и оптимизировать компоненты с повторяющейся логикой
- ✅ Вынести общие паттерны в утилиты

**Критерии готовности:**
- Компоненты стали проще
- Устранено дублирование логики

---

## ✅ Текущий прогресс

### Фаза 1: Унификация работы с API ✅ ЗАВЕРШЕНО

- [x] Шаг 1.1: Создать API клиент ✅
  - Создан `frontend/src/services/apiClient.ts`
  - Реализованы все методы для endpoints
  - Вынесена логика dev режима
  - Единая обработка ошибок

- [x] Шаг 1.2: Мигрировать компоненты на API клиент ✅
  - Мигрировано 9 файлов
  - Устранено ~200 строк дублирующегося кода
  - Все fetch вызовы заменены на api методы

### Фаза 2: Унификация типов и интерфейсов ✅ ЗАВЕРШЕНО

- [x] Шаг 2.1: Устранить дублирование типов ✅
  - Удалены дубликаты `IndexProjectRequest/Response`
  - Централизованы типы валидации в `types/api.ts`
  - Обновлены импорты во всех файлах

- [x] Шаг 2.2: Создать кастомные хуки ✅
  - Создан `hooks/useApi.ts` - универсальный хук для API запросов
  - Создан `hooks/useModels.ts` - хук для работы с моделями
  - Создан `hooks/useLocalStorage.ts` - хук для работы с localStorage
  - Мигрировано 4 компонента на использование хуков

### Фаза 3: Оптимизация компонентов ✅ ЗАВЕРШЕНО

- [x] Шаг 3.1: Оптимизация компонентов ✅
  - Созданы общие UI компоненты (LoadingState, ErrorState, EmptyState)
  - Создан helper для SSE обработчиков (sseHelpers.ts)
  - Оптимизирован MetricsDashboard (миграция на useApi)
  - Оптимизирован useAgentStream (использование helper функций)
  - Устранено ~160 строк дублирующегося кода

---

## 📝 Детальный план Шага 1.1: API клиент

### Структура `apiClient.ts`:

```typescript
// frontend/src/services/apiClient.ts

/**
 * Централизованный API клиент для всех запросов к backend.
 * 
 * Обеспечивает:
 * - Единую обработку ошибок
 * - Автоматическое определение dev/prod режима
 * - Типобезопасные методы для всех endpoints
 */

// Определение базового URL
function getBaseUrl(): string {
  const isDev = typeof window !== 'undefined' && window.location.port === '5173'
  return isDev ? 'http://localhost:8000' : ''
}

// Методы для разных типов запросов
export const apiClient = {
  // GET запросы
  get: async <T>(endpoint: string): Promise<T> => { ... },
  
  // POST запросы
  post: async <T>(endpoint: string, body?: unknown): Promise<T> => { ... },
  
  // DELETE запросы
  delete: async <T>(endpoint: string): Promise<T> => { ... },
  
  // SSE соединение (для стриминга)
  createSSE: (endpoint: string, params: URLSearchParams): EventSource => { ... }
}

// Специфичные методы для каждого endpoint
export const api = {
  models: {
    list: () => apiClient.get<ModelsResponse>('/api/models'),
    // ...
  },
  conversations: {
    list: () => apiClient.get<ConversationsListResponse>('/api/conversations'),
    get: (id: string) => apiClient.get<ConversationResponse>(`/api/conversations/${id}`),
    delete: (id: string) => apiClient.delete(`/api/conversations/${id}`),
    // ...
  },
  // ... остальные endpoints
}
```

---

## 📝 Детальный план Шага 1.2: Миграция компонентов

### Порядок миграции:

1. **`modelUtils.ts`** - простой, используется в нескольких местах
2. **`ChatHistory.tsx`** - простой компонент
3. **`MetricsDashboard.tsx`** - простой компонент
4. **`MessageList.tsx`** - простой компонент
5. **`SidebarOptions.tsx`** - простой компонент
6. **`App.tsx`** - основной компонент
7. **`IDEPanel.tsx`** - сложный компонент с множеством запросов
8. **`useCodeExecution.ts`** - хук

---

## 📝 Детальный план Шага 2.1: Устранение дублирования типов

### Действия:

1. **Удалить `ValidationResult` из `useCodeExecution.ts`**
   - Использовать тип из `useAgentStream.ts` или вынести в `types/api.ts`

2. **Удалить дублирующиеся `IndexProjectRequest/Response`**
   - Оставить только в `types/api.ts` (строки 97-109)
   - Удалить дубликаты (строки 206-218)

3. **Проверить другие дублирующиеся типы**
   - `AgentResults` в тестах - использовать из `useAgentStream.ts`

---

## 📝 Детальный план Шага 2.2: Кастомные хуки

### `hooks/useApi.ts`:

```typescript
/**
 * Универсальный хук для API запросов с состоянием загрузки.
 */
export function useApi<T>(
  fetcher: () => Promise<T>,
  deps: DependencyList = []
): {
  data: T | null
  loading: boolean
  error: string | null
  refetch: () => Promise<void>
}
```

### `hooks/useModels.ts`:

```typescript
/**
 * Хук для работы с моделями (кэширование, загрузка).
 */
export function useModels(): {
  models: string[]
  modelsDetailed: ModelInfo[]
  loading: boolean
  error: string | null
  isReasoningModel: (name: string) => boolean
  refetch: () => Promise<void>
}
```

### `hooks/useLocalStorage.ts`:

```typescript
/**
 * Хук для работы с localStorage с типизацией.
 */
export function useLocalStorage<T>(
  key: string,
  initialValue: T
): [T, (value: T) => void, () => void]
```

---

## 📊 Оценка времени

| Фаза | Шаг | Время | Приоритет |
|------|-----|-------|-----------|
| 1 | 1.1: API клиент | 2-3 ч | 🔴 Критично |
| 1 | 1.2: Миграция компонентов | 3-4 ч | 🔴 Критично |
| 2 | 2.1: Устранение типов | 1-2 ч | 🟡 Средне |
| 2 | 2.2: Кастомные хуки | 2-3 ч | 🟡 Средне |
| 3 | 3.1: Оптимизация | 2-3 ч | 🟢 Низко |

**Итого:** 10-15 часов

---

## ✅ Критерии успеха

- [ ] Все API вызовы используют единый клиент
- [ ] Нет дублирующихся типов
- [ ] Единая обработка ошибок везде
- [ ] Компоненты стали проще и понятнее
- [ ] Устранено ~200-300 строк дублирующегося кода
- [ ] TypeScript компилируется без ошибок
- [ ] Линтер не находит проблем

---

## 📝 Заметки

- Следовать правилам из `.cursor/rules/`
- TypeScript типы обязательны
- Обратная совместимость должна быть сохранена
- Тесты должны проходить после каждого шага
