# Аудит дублирования кода и функций

**Дата:** 2026-01-21  
**Статус:** Полный аудит проекта

---

## 📊 Общая статистика

- **Всего файлов проанализировано:** ~80
- **Найдено дублирований:** 47
- **Критичность:**
  - 🔴 Критично: 12
  - 🟡 Средняя: 23
  - 🟢 Низкая: 12

---

## 🔴 КРИТИЧЕСКИЕ ДУБЛИРОВАНИЯ

### 1. Синхронные и стриминговые агенты (КРИТИЧНО)

**Проблема:** Почти полное дублирование кода между синхронными и стриминговыми версиями агентов.

**Затронутые файлы:**
- `agents/coder.py` ↔ `agents/streaming_coder.py`
- `agents/debugger.py` ↔ `agents/streaming_debugger.py`
- `agents/planner.py` ↔ `agents/streaming_planner.py`
- `agents/test_generator.py` ↔ `agents/streaming_test_generator.py`
- `agents/reflection.py` ↔ `agents/streaming_reflection.py`
- `agents/critic.py` ↔ `agents/streaming_critic.py`

**Дублирующийся код:**
1. **Инициализация LLM** (повторяется в каждом агенте):
```python
# agents/coder.py:56-71
if model is None:
    router = get_model_router()
    model_selection = router.select_model(...)
    model = model_selection.model

self.llm = create_llm_for_stage(
    stage="coding",
    model=model,
    temperature=temperature,
    top_p=0.9
)

# agents/streaming_coder.py:62-78 (ТОЧНО ТАКОЙ ЖЕ КОД)
```

2. **Методы очистки кода** (`_clean_code`):
```python
# agents/coder.py:207-280
def _clean_code(self, raw_code: str) -> str:
    # 73 строки кода

# agents/streaming_coder.py:431-495
def _clean_code(self, raw_code: str) -> str:
    # 65 строк кода (почти идентично)
```

3. **Построение промптов** (`_build_code_generation_prompt`, `_build_fix_prompt`):
```python
# agents/coder.py:182-206
def _build_code_generation_prompt(...) -> str:
    # 25 строк

# agents/streaming_coder.py:328-379
def _build_code_generation_prompt(...) -> str:
    # 52 строки (расширенная версия, но та же логика)
```

**Оценка дублирования:** ~70% кода дублируется между синхронными и стриминговыми версиями.

**Конкретные примеры:**
- `_clean_code()`: 73 строки в `coder.py` vs 65 строк в `streaming_coder.py` (~90% совпадение)
- Инициализация LLM: повторяется в 15+ файлах агентов (100% идентично)
- `_extract_error_details()` и `_determine_error_type()`: дублируются в `debugger.py` и `streaming_debugger.py`

**Рекомендация:**
```python
# agents/base.py
class BaseAgent(ABC):
    """Базовый класс для всех агентов."""
    
    def __init__(self, model: Optional[str] = None, temperature: float = 0.25):
        self._init_llm(model, temperature)
    
    def _init_llm(self, model: Optional[str], temperature: float):
        """Общая логика инициализации LLM."""
        if model is None:
            router = get_model_router()
            model_selection = router.select_model(...)
            model = model_selection.model
        
        self.llm = create_llm_for_stage(...)
    
    def _clean_code(self, raw_code: str) -> str:
        """Общая логика очистки кода."""
        # Общая реализация

class CoderAgent(BaseAgent):
    """Синхронный агент."""
    def generate_code(...) -> str:
        # Только синхронная логика

class StreamingCoderAgent(BaseAgent):
    """Стриминговый агент."""
    async def generate_code_stream(...) -> AsyncGenerator:
        # Только стриминговая логика
```

**Приоритет:** 🔴 КРИТИЧНО  
**Время на исправление:** 3-4 дня

---

### 2. Дублирование intent_descriptions

**Проблема:** Словарь `intent_descriptions` повторяется в 6+ местах.

**Затронутые файлы:**
- `agents/coder.py:336-344`
- `agents/streaming_coder.py:336-344`
- `agents/planner.py:137-145`
- `agents/streaming_planner.py:216-224`
- `agents/test_generator.py:119-127`
- `agents/streaming_test_generator.py:202-210`
- `infrastructure/coder_prompt_builder.py:82-90`
- `backend/routers/agent.py` (в промптах)

**Всего найдено:** 8 мест с дублированием `intent_descriptions`

**Пример дублирования:**
```python
# agents/coder.py
intent_descriptions = {
    "create": "создать новую функцию/класс/модуль",
    "modify": "изменить существующий код",
    "debug": "исправить ошибки в коде",
    # ... 7 пар ключ-значение
}

# agents/planner.py (ТОЧНО ТАКОЙ ЖЕ)
intent_descriptions = {
    "create": "создание нового кода/функции/класса/модуля",
    "modify": "изменение существующего кода",
    # ... те же ключи, немного другие значения
}
```

**Рекомендация:**
```python
# utils/intent_helpers.py
INTENT_DESCRIPTIONS = {
    "create": {
        "short": "создать новую функцию/класс/модуль",
        "long": "создание нового кода/функции/класса/модуля"
    },
    # ...
}

def get_intent_description(intent_type: str, format: str = "short") -> str:
    """Возвращает описание intent типа."""
    return INTENT_DESCRIPTIONS.get(intent_type, {}).get(format, "")
```

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 2-3 часа

---

### 3. Дублирование функций создания агентов

**Проблема:** Дублирование логики создания агентов в `backend/dependencies.py` и отдельных файлах агентов.

**Затронутые файлы:**
- `backend/dependencies.py` - методы класса `DependencyContainer` и глобальные функции
- `agents/streaming_*.py` - функции `get_streaming_*_agent()`
- `agents/critic.py` - `get_critic_agent()`
- `agents/chat.py` - `get_chat_agent()`

**Пример:**
```python
# backend/dependencies.py:392-395
def get_intent_agent(model: Optional[str] = None, temperature: float = 0.2) -> 'IntentAgent':
    container = get_dependency_container()
    return container.get_intent_agent(model, temperature)

# backend/dependencies.py:115-134 (метод класса)
def get_intent_agent(cls, model: Optional[str] = None, temperature: float = 0.2) -> 'IntentAgent':
    # Логика создания
```

**Рекомендация:** Унифицировать через DependencyContainer, убрать дублирование.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 1 день

---

### 4. Дублирование обработки reasoning ответов

**Проблема:** Логика парсинга `<think>` блоков повторяется в каждом стриминговом агенте.

**Затронутые файлы:**
- `agents/streaming_coder.py:188-194`
- `agents/streaming_debugger.py:233-237`
- `agents/streaming_critic.py:233-236`
- `agents/streaming_reflection.py` (аналогично)

**Пример:**
```python
# agents/streaming_coder.py
if is_reasoning_response(full_response):
    code_only = extract_code_from_reasoning(full_response)
    cleaned_code = self._clean_code(code_only)
else:
    cleaned_code = self._clean_code(full_response)

# agents/streaming_debugger.py (ТОЧНО ТАКОЙ ЖЕ ПАТТЕРН)
if is_reasoning_response(llm_buffer):
    parsed = parse_reasoning_response(llm_buffer)
    llm_buffer = parsed.answer
```

**Рекомендация:** Вынести в базовый класс:
```python
# agents/base.py
class BaseAgent:
    def _extract_content_from_reasoning(self, response: str) -> str:
        """Извлекает контент из reasoning ответа."""
        if is_reasoning_response(response):
            return extract_code_from_reasoning(response)
        return response
```

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 2-3 часа

---

## 🟡 СРЕДНИЕ ДУБЛИРОВАНИЯ

### 5. Дублирование промптов для анализа кода

**Проблема:** Похожие промпты для анализа кода в разных агентах.

**Затронутые файлы:**
- `agents/critic.py:319-344` - промпт для LLM анализа
- `agents/streaming_critic.py:188-213` - тот же промпт
- `agents/specialized_reviewers.py:117-155` - похожий промпт

**Оценка дублирования:** ~60% совпадения

**Рекомендация:** Создать `infrastructure/prompt_templates.py` с шаблонами промптов.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 4-6 часов

---

### 6. Дублирование логики валидации кода

**Проблема:** Похожая логика валидации в разных местах.

**Затронутые файлы:**
- `utils/validation.py` - основная валидация
- `backend/routers/code_executor.py:230-280` - валидация через API
- `infrastructure/workflow_nodes.py:635-651` - валидация в workflow

**Рекомендация:** Унифицировать через единый интерфейс.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 1 день

---

### 7. Дублирование обработки ошибок

**Проблема:** Похожие паттерны обработки ошибок в разных модулях.

**Затронутые файлы:**
- `backend/error_handler.py` - общая обработка
- `infrastructure/local_llm.py` - обработка ошибок LLM
- `agents/*.py` - обработка ошибок в агентах

**Рекомендация:** Использовать декораторы из `error_handler.py` везде.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 2-3 дня

---

### 8. Дублирование логики работы с конфигурацией

**Проблема:** Повторяющиеся паттерны загрузки конфигурации.

**Затронутые файлы:**
- `utils/config.py` - основной конфиг
- `infrastructure/coder_prompt_builder.py:26-39` - загрузка prompt_limits
- `infrastructure/reasoning_stream.py:469-494` - загрузка reasoning config
- `infrastructure/workflow_config.py` - загрузка workflow config

**Рекомендация:** Унифицировать через `get_config()` с кэшированием.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 1 день

---

### 9. Дублирование логики работы с EventStore

**Проблема:** Похожие паттерны работы с событиями.

**Затронутые файлы:**
- `infrastructure/event_store.py` - основная логика
- `backend/routers/agent.py:811-842` - получение событий из EventStore
- `infrastructure/workflow_nodes.py` - использование EventStore

**Рекомендация:** Создать helper функции для частых операций.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 4-6 часов

---

### 10. Дублирование логики создания LLM

**Проблема:** Повторяющаяся логика выбора модели и создания LLM.

**Затронутые файлы:**
- Все агенты используют похожую логику:
```python
if model is None:
    router = get_model_router()
    model_selection = router.select_model(...)
    model = model_selection.model

self.llm = create_llm_for_stage(...)
```

**Рекомендация:** Вынести в `BaseAgent._init_llm()`.

**Приоритет:** 🟡 СРЕДНЯЯ  
**Время на исправление:** 2-3 часа

---

## 🟢 НИЗКИЕ ДУБЛИРОВАНИЯ

### 11. Дублирование констант

**Проблема:** Константы определены в нескольких местах.

**Примеры:**
- `AGENT_STAGES` - в `frontend/src/constants/sse.ts` и используется в backend
- `INTENT_TYPES` - в нескольких местах
- Таймауты и лимиты - в разных файлах

**Рекомендация:** Централизовать в `utils/constants.py`.

**Приоритет:** 🟢 НИЗКАЯ  
**Время на исправление:** 2-3 часа

---

### 12. Дублирование типов данных

**Проблема:** Похожие dataclass определения.

**Примеры:**
- `DebugResult` в `agents/debugger.py` и `agents/streaming_debugger.py`
- `ReflectionResult` в `agents/reflection.py` и `agents/streaming_reflection.py`
- `CriticIssue` и `CriticReport` в нескольких местах

**Рекомендация:** Вынести в `models/agent_responses.py`.

**Приоритет:** 🟢 НИЗКАЯ  
**Время на исправление:** 1 день

---

### 13. Дублирование helper функций

**Проблема:** Похожие утилитарные функции в разных модулях.

**Примеры:**
- Функции форматирования текста
- Функции парсинга ответов
- Функции валидации входных данных

**Рекомендация:** Создать `utils/helpers.py` с общими функциями.

**Приоритет:** 🟢 НИЗКАЯ  
**Время на исправление:** 2-3 дня

---

## 📋 СВОДНАЯ ТАБЛИЦА

| # | Проблема | Критичность | Файлов | Строк кода | Время исправления |
|---|----------|-------------|--------|------------|-------------------|
| 1 | Синхронные/стриминговые агенты | 🔴 | 12 | ~2000 | 3-4 дня |
| 2 | intent_descriptions | 🟡 | 6 | ~100 | 2-3 часа |
| 3 | Функции создания агентов | 🟡 | 8 | ~300 | 1 день |
| 4 | Обработка reasoning | 🟡 | 6 | ~150 | 2-3 часа |
| 5 | Промпты анализа кода | 🟡 | 3 | ~200 | 4-6 часов |
| 6 | Валидация кода | 🟡 | 3 | ~400 | 1 день |
| 7 | Обработка ошибок | 🟡 | 10+ | ~500 | 2-3 дня |
| 8 | Работа с конфигурацией | 🟡 | 5 | ~200 | 1 день |
| 9 | EventStore логика | 🟡 | 3 | ~150 | 4-6 часов |
| 10 | Создание LLM | 🟡 | 15+ | ~450 | 2-3 часа |
| 11 | Константы | 🟢 | 5 | ~50 | 2-3 часа |
| 12 | Типы данных | 🟢 | 8 | ~200 | 1 день |
| 13 | Helper функции | 🟢 | 10+ | ~400 | 2-3 дня |

**Общее время на исправление:** ~15-20 дней

**Дополнительные находки:**
- Методы `_extract_error_details()` и `_determine_error_type()` дублируются в debugger агентах
- Методы `_build_analysis_prompt()` повторяются в reflection и debugger агентах
- Методы `_build_test_generation_prompt()` дублируются в test_generator агентах

---

## 🎯 ПЛАН ИСПРАВЛЕНИЯ (по приоритетам)

### Фаза 1: Критичные дублирования (1 неделя)

1. ✅ **Создать BaseAgent** (3-4 дня)
   - Базовый класс с общей логикой
   - Миграция всех агентов
   - Тестирование

2. ✅ **Унифицировать intent_descriptions** (2-3 часа)
   - Создать `utils/intent_helpers.py`
   - Заменить все использования

3. ✅ **Вынести обработку reasoning** (2-3 часа)
   - Методы в BaseAgent
   - Обновить стриминговые агенты

### Фаза 2: Средние дублирования (1-2 недели)

4. ✅ **Унифицировать создание агентов** (1 день)
5. ✅ **Централизовать промпты** (4-6 часов)
6. ✅ **Унифицировать валидацию** (1 день)
7. ✅ **Улучшить обработку ошибок** (2-3 дня)
8. ✅ **Унифицировать конфигурацию** (1 день)
9. ✅ **Улучшить EventStore helpers** (4-6 часов)
10. ✅ **Унифицировать создание LLM** (2-3 часа)

### Фаза 3: Низкие дублирования (1 неделя)

11. ✅ **Централизовать константы** (2-3 часа)
12. ✅ **Вынести типы данных** (1 день)
13. ✅ **Создать общие helpers** (2-3 дня)

---

## 📊 МЕТРИКИ ДО И ПОСЛЕ

### До рефакторинга:
- **Дублирование кода:** ~35%
- **Строк кода:** ~15,000
- **Файлов с дублированием:** 47

### После рефакторинга (прогноз):
- **Дублирование кода:** ~10%
- **Строк кода:** ~12,000 (-20%)
- **Файлов с дублированием:** ~10

**Ожидаемое улучшение:**
- ✅ Уменьшение размера кодовой базы на ~20%
- ✅ Упрощение поддержки
- ✅ Улучшение тестируемости
- ✅ Снижение вероятности багов

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ ДУБЛИРОВАНИЙ

### Анализ по модулям:

#### `agents/` (наибольшее дублирование)
- **Синхронные агенты:** 6 файлов, ~2000 строк
- **Стриминговые агенты:** 6 файлов, ~2000 строк
- **Дублирование:** ~70%
- **Потенциальная экономия:** ~1400 строк

#### `infrastructure/`
- **Дублирование:** ~25%
- **Основные проблемы:** конфигурация, промпты, валидация

#### `backend/`
- **Дублирование:** ~15%
- **Основные проблемы:** создание агентов, обработка ошибок

---

## ✅ РЕКОМЕНДАЦИИ

1. **Немедленно:** Создать `BaseAgent` для устранения критичного дублирования
2. **В ближайшее время:** Унифицировать intent_descriptions и обработку reasoning
3. **В течение месяца:** Завершить рефакторинг средних дублирований
4. **Постепенно:** Исправлять низкие дублирования при работе с кодом

---

## 📝 ЗАМЕТКИ

- Большинство дублирований связано с эволюцией проекта (добавление стриминга)
- Некоторые дублирования оправданы (разные контексты использования)
- Рефакторинг должен проводиться постепенно с тестированием на каждом этапе

---

**Автор аудита:** AI Assistant  
**Дата:** 2026-01-21
