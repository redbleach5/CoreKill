# 🔴 Критические проблемы проекта

**Дата анализа:** 2026-01-23  
**Статус:** Требуют немедленного внимания

---

## 🔴 КРИТИЧЕСКИЕ (Требуют немедленного исправления)

### 1. ✅ ИСПРАВЛЕНО: Использование `exec()` для выполнения кода (КРИТИЧЕСКАЯ УЯЗВИМОСТЬ БЕЗОПАСНОСТИ)

**Файл:** `utils/validation.py:351-444`  
**Статус:** ✅ Исправлено 2026-01-23

**Проблема:**
```python
# Выполняем код
exec(code, namespace)

# Выполняем тесты
exec(tests, namespace)
```

**Контекст:**
- Функция `validate_code()` используется для валидации сгенерированного кода
- Хотя есть проверки синтаксиса и компиляции, финальное выполнение через `exec()` остается опасным
- Используется пустой `namespace`, но это не защищает от всех атак

**Риски:**
- ⚠️ **КРИТИЧЕСКИЙ РИСК БЕЗОПАСНОСТИ**: `exec()` может выполнить произвольный код
- Даже с проверками безопасности, `exec()` остается опасным
- Нет изоляции выполнения (sandbox)
- Может привести к RCE (Remote Code Execution) при эксплуатации уязвимостей
- Потенциальный доступ к файловой системе, сети, системным ресурсам

**Текущие меры защиты:**
- ✅ Проверка синтаксиса через `ast.parse()`
- ✅ Проверка компиляции через `compile()`
- ✅ Пустой namespace для изоляции
- ❌ Нет ограничений на время выполнения
- ❌ Нет ограничений на использование ресурсов
- ❌ Нет полной изоляции процесса

**Решение (РЕАЛИЗОВАНО):**
1. ✅ Заменён `exec()` на `subprocess.run()` с изоляцией процесса
2. ✅ Добавлен таймаут (5 секунд по умолчанию)
3. ✅ Ограничен размер вывода ошибок (500 символов)
4. ✅ Используются временные файлы для изоляции
5. ✅ Сохранена обратная совместимость API
6. ✅ Все тесты проходят успешно

**Реализация:**
- Создана функция `_execute_code_safely()` которая использует subprocess
- Код и тесты выполняются в изолированном процессе
- Используется импорт модуля вместо exec() для безопасности

**Связанные файлы:**
- `backend/routers/code_executor.py` - использует `subprocess` (более безопасно)
- `backend/routers/code_executor.py:115-226` - есть проверки безопасности, но тоже требует улучшения

**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Сложность:** Средняя

---

### 2. 94 "Orphan" модуля (Мертвый код / Неиспользуемые зависимости)

**Источник:** `dependency_analysis.json`

**Проблема:**
94 модуля не импортируются нигде в проекте через статические импорты:
- Все агенты в `agents/` (21 файл): `chat.py`, `coder.py`, `conversation.py`, `critic.py`, `debugger.py`, и т.д.
- Большинство backend модулей (18 файлов): `api.py`, `dependencies.py`, `error_handler.py`, и т.д.
- Множество infrastructure модулей (55+ файлов): `agent_resource_manager.py`, `ast_analyzer.py`, `cache.py`, и т.д.

**Важно:** Некоторые модули могут использоваться через:
- Динамические импорты (`importlib`)
- Декораторы и метаклассы
- LangGraph workflow (автоматическая загрузка)
- FastAPI dependency injection

**Риски:**
- Увеличение размера проекта и времени сборки
- Путаница в архитектуре (непонятно что используется)
- Сложность поддержки (неясно что можно безопасно удалить)
- Возможные неиспользуемые зависимости в `requirements.txt`
- Увеличение времени загрузки приложения

**Решение:**
1. Провести детальный аудит: действительно ли эти модули не используются?
2. Проверить динамические импорты через `grep -r "importlib\|__import__"`
3. Проверить использование через декораторы и метаклассы
4. Если используются - задокументировать способ использования
5. Если не используются - удалить или переместить в `archive/`
6. Обновить `dependency_analysis.json` после очистки
7. Добавить в CI/CD проверку на неиспользуемые импорты

**Файл:** `dependency_analysis.json`

**Проблема:**
94 модуля не импортируются нигде в проекте:
- Все агенты в `agents/` (21 файл)
- Большинство backend модулей (18 файлов)
- Множество infrastructure модулей (55+ файлов)

**Риски:**
- Увеличение размера проекта
- Путаница в архитектуре
- Сложность поддержки
- Возможные неиспользуемые зависимости

**Решение:**
1. Провести аудит: действительно ли эти модули не используются?
2. Если используются через динамические импорты - задокументировать
3. Если не используются - удалить или переместить в архив
4. Обновить dependency_analysis.json после очистки

**Приоритет:** 🟡 ВЫСОКИЙ  
**Сложность:** Низкая

---

### 3. ✅ ИСПРАВЛЕНО: Потенциальные утечки памяти в ConfidenceAccumulator

**Файл:** `infrastructure/autonomous_improver/confidence_accumulator.py`  
**Статус:** ✅ Исправлено 2026-01-23

**Проблема:**
- История накапливалась без ограничений
- `clear_old_history()` вызывалась только при переполнении
- При большом количестве файлов могла привести к утечке памяти

**Решение (РЕАЛИЗОВАНО):**
- ✅ Добавлена периодическая очистка (каждые N вызовов update)
- ✅ Уже есть ограничение размера истории (max_history_size)
- ✅ Улучшена очистка связанных file_hashes
- ✅ Автоматическое удаление старых записей при переполнении

---

## 🟡 ВАЖНЫЕ ПРОБЛЕМЫ

### 4. ✅ ИСПРАВЛЕНО: Слабая обработка ошибок парсинга JSON от LLM

**Файл:** `infrastructure/autonomous_improver/core.py:1301-1397`  
**Статус:** ✅ Исправлено 2026-01-23

**Проблема:**
- При невалидном JSON от LLM просто возвращался пустой список
- Не было попыток исправить JSON (убрать markdown, найти JSON в тексте)
- Не было накопления статистики ошибок парсинга

**Решение (РЕАЛИЗОВАНО):**
- ✅ Использование улучшенного парсера `parse_llm_json_response()` из `json_parser.py`
- ✅ Автоматическое извлечение JSON из markdown блоков
- ✅ Исправление распространенных ошибок JSON (trailing commas, одинарные кавычки)
- ✅ Fallback на regex парсинг при ошибке JSONDecodeError
- ✅ Улучшенное логирование для мониторинга

---

### 5. Отсутствие проверки на закоммиченные секреты

**Проблема:**
- `.env` в `.gitignore`, но нет проверки что секреты не закоммичены
- API ключи могут быть в истории коммитов
- Нет автоматической проверки в CI/CD

**Решение:**
1. Использовать `git-secrets` или `truffleHog` для сканирования
2. Добавить pre-commit hook
3. Проверить историю коммитов на наличие секретов
4. Ротация ключей если найдены

**Приоритет:** 🟡 СРЕДНИЙ  
**Сложность:** Низкая

---

### 6. Отсутствие requirements.txt в .gitignore

**Проблема:**
- `requirements.txt` закоммичен, но генерируется из `requirements.in`
- Может привести к конфликтам при разных версиях Python/pip-compile

**Решение:**
- Добавить `requirements.txt` в `.gitignore`
- Генерировать через CI/CD или в документации
- Или явно указать что файл должен быть закоммичен

**Приоритет:** 🟢 НИЗКИЙ  
**Сложность:** Очень низкая

---

## 🔵 АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### 7. Смешение синхронных и асинхронных паттернов

**Проблема:**
- Некоторые агенты синхронные, другие асинхронные
- Использование `asyncio.to_thread()` для обертки синхронного кода
- Может привести к проблемам производительности

**Решение:**
- Унифицировать на асинхронные паттерны
- Или четко разделить синхронные и асинхронные компоненты

**Приоритет:** 🟢 НИЗКИЙ  
**Сложность:** Высокая

---

### 8. Отсутствие явных ограничений на размер входных данных

**Проблема:**
- Есть `MAX_PROMPT_LENGTH = 50000`, но нет ограничений на:
  - Размер файлов для анализа
  - Количество файлов в проекте
  - Размер контекста для LLM

**Решение:**
- Добавить явные лимиты в конфигурацию
- Валидация на входе API
- Graceful degradation при превышении лимитов

**Приоритет:** 🟢 НИЗКИЙ  
**Сложность:** Средняя

---

## 📊 Статистика проблем

- 🔴 Критические: 1
- 🟡 Важные: 5
- 🔵 Архитектурные: 2

**Итого:** 8 проблем требуют внимания

---

## 🎯 Рекомендуемый порядок исправления

1. **Немедленно:** Исправить использование `exec()` (#1)
2. **В течение недели:** Провести аудит orphan модулей (#2)
3. **В течение месяца:** Исправить утечки памяти (#3), улучшить обработку JSON (#4)
4. **Постепенно:** Остальные проблемы

---

## 📝 Примечания

- Большинство проблем уже частично решены или имеют workarounds
- Проект имеет хорошую архитектуру и документацию
- Критические проблемы безопасности требуют немедленного внимания
