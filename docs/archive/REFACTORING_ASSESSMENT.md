# Оценка необходимости рефакторинга проблемных модулей

## Метрики качества кода

### Coverage по модулям:
- `error_handler.py`: **0%** (149 строк)
- `code_executor.py`: **30%** (262 строки) 
- `database.py`: **28%** (256 строк)
- `agent.py`: **49%** (2278 строк!)
- `dependencies.py`: **68%** (714 строк)

---

## 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ

### 1. `backend/error_handler.py` (0% coverage)

**Проблемы:**
- ❌ **КРИТИЧЕСКАЯ ОШИБКА**: `sync_retry` использует `asyncio.run(asyncio.sleep(delay))` внутри синхронной функции
  - Это блокирует event loop и может вызвать `RuntimeError: asyncio.run() cannot be called from a running event loop`
  - Антипаттерн: смешивание sync/async кода
- ❌ Полностью не покрыт тестами
- ❌ Используется в критических местах (retry логика)

**Необходимость рефакторинга:** 🔴 **КРИТИЧЕСКАЯ**

**Рекомендации:**
1. Исправить `sync_retry` - использовать `time.sleep()` вместо `asyncio.run()`
2. Добавить unit-тесты для обоих декораторов
3. Добавить интеграционные тесты с реальными retry сценариями
4. Рассмотреть использование библиотеки `tenacity` вместо самописной реализации

**Оценка сложности:** Низкая (1-2 часа)

---

### 2. `backend/routers/code_executor.py` (30% coverage)

**Проблемы:**
- ❌ **КРИТИЧЕСКАЯ УЯЗВИМОСТЬ БЕЗОПАСНОСТИ**: Проверка безопасности через простые строковые паттерны
  - Легко обойти: `import os as o`, `eval("__import__('os')")`, `getattr(__builtins__, '__import__')('os')`
  - Нет реальной изоляции - просто subprocess без sandbox
  - Можно выполнить опасный код через обход проверок
- ❌ Нет ограничений по памяти (только по времени)
- ❌ Нет проверки на бесконечные циклы (только timeout)
- ❌ Временные файлы могут остаться при ошибках

**Необходимость рефакторинга:** 🔴 **КРИТИЧЕСКАЯ** (безопасность)

**Рекомендации:**
1. Использовать AST парсинг вместо строковых паттернов для проверки безопасности
2. Добавить реальную изоляцию (Docker контейнер или `pysandbox`)
3. Добавить ограничения по памяти через `resource` модуль
4. Улучшить очистку временных файлов (context manager)
5. Добавить тесты для всех edge cases безопасности

**Оценка сложности:** Высокая (1-2 дня)

---

## 🟡 ВЫСОКИЙ ПРИОРИТЕТ

### 3. `backend/routers/agent.py` (49% coverage, 2278 строк!)

**Проблемы:**
- ❌ **Нарушение Single Responsibility Principle**: Огромный файл с множеством ответственностей
  - Обработка разных режимов (chat, analyze, workflow)
  - SSE стриминг
  - Выбор моделей
  - Интеграция с агентами
- ❌ Дублирование кода между `run_chat_stream`, `run_analyze_stream`, `run_workflow_stream`
- ❌ Сложно тестировать из-за размера
- ❌ Сложно поддерживать и расширять
- ❌ Много не покрытого кода (406 строк не покрыто)

**Необходимость рефакторинга:** 🟡 **ВЫСОКАЯ** (архитектура)

**Рекомендации:**
1. Разделить на отдельные модули:
   - `agent_chat_handler.py` - обработка chat режима
   - `agent_analyze_handler.py` - обработка analyze режима
   - `agent_workflow_handler.py` - обработка workflow режима
   - `agent_model_selector.py` - логика выбора моделей
   - `agent_stream_manager.py` - управление SSE стримингом
2. Вынести общую логику в базовые классы/функции
3. Использовать Strategy pattern для разных режимов
4. Улучшить покрытие тестами после рефакторинга

**Оценка сложности:** Средняя-Высокая (2-3 дня)

---

## 🟢 СРЕДНИЙ ПРИОРИТЕТ

### 4. `backend/routers/database.py` (28% coverage)

**Проблемы:**
- ⚠️ Низкое покрытие тестами
- ⚠️ Нет обработки edge cases (недостаточно места на диске, проблемы с правами доступа)
- ⚠️ Нет валидации путей для backup/restore

**Необходимость рефакторинга:** 🟢 **СРЕДНЯЯ**

**Рекомендации:**
1. Добавить больше тестов для edge cases
2. Улучшить обработку ошибок
3. Добавить валидацию путей и проверку доступного места

**Оценка сложности:** Низкая (4-6 часов)

---

### 5. `backend/dependencies.py` (68% coverage, 714 строк)

**Проблемы:**
- ⚠️ Большой файл с дублированием кода
- ⚠️ Много похожих методов `get_*_agent` с одинаковой логикой
- ⚠️ Можно использовать generics для уменьшения дублирования

**Необходимость рефакторинга:** 🟢 **СРЕДНЯЯ**

**Рекомендации:**
1. Использовать generic методы для создания агентов
2. Вынести общую логику кэширования
3. Улучшить покрытие тестами

**Оценка сложности:** Средняя (1 день)

---

## 📊 Сводная таблица приоритетов

| Модуль | Coverage | Строк | Приоритет | Критичность | Сложность | Время |
|--------|----------|-------|-----------|--------------|-----------|-------|
| `error_handler.py` | 0% | 149 | 🔴 Критический | Высокая | Низкая | 1-2ч |
| `code_executor.py` | 30% | 262 | 🔴 Критический | **Безопасность** | Высокая | 1-2 дня |
| `agent.py` | 49% | 2278 | 🟡 Высокий | Архитектура | Средняя-Высокая | 2-3 дня |
| `database.py` | 28% | 256 | 🟢 Средний | Низкая | Низкая | 4-6ч |
| `dependencies.py` | 68% | 714 | 🟢 Средний | Низкая | Средняя | 1 день |

---

## 🎯 Рекомендуемый порядок рефакторинга

### Фаза 1: Критические исправления (1-2 дня)
1. ✅ Исправить `error_handler.py` - критическая ошибка с `asyncio.run()`
2. ✅ Улучшить безопасность `code_executor.py` - AST парсинг, изоляция

### Фаза 2: Архитектурные улучшения (2-3 дня)
3. ✅ Разделить `agent.py` на модули
4. ✅ Рефакторинг `dependencies.py` для уменьшения дублирования

### Фаза 3: Улучшение качества (1 день)
5. ✅ Улучшить `database.py` - edge cases, валидация

---

## 💡 Дополнительные рекомендации

1. **Добавить pre-commit hooks** для проверки coverage перед коммитом
2. **Установить минимальный порог coverage** (например, 70%) для новых модулей
3. **Использовать type hints** везде для лучшей читаемости
4. **Добавить документацию** для сложных модулей
5. **Рассмотреть использование линтеров** (ruff, pylint) для автоматического обнаружения проблем

---

## ⚠️ Риски при рефакторинге

1. **`agent.py`** - большой риск регрессий из-за размера и сложности
   - Решение: тщательное тестирование после каждого шага рефакторинга
   
2. **`code_executor.py`** - изменения безопасности могут сломать существующий функционал
   - Решение: добавить feature flags для постепенного внедрения

3. **`error_handler.py`** - используется в критических местах
   - Решение: сначала добавить тесты, потом исправлять

---

**Дата анализа:** 2026-01-23
**Аналитик:** AI Assistant
