# Отчет о дублирующемся коде и легаси коде

**Дата:** 2026-01-25  
**Статус:** Актуальный анализ

---

## 📊 Общая статистика

- **Проанализировано файлов:** 204 Python файла
- **Всего функций:** 1660
- **Дублирующиеся функции (точные копии):** 13
- **Похожие функции (по имени):** 53
- **Файлов с легаси маркерами:** 14
- **Всего легаси маркеров:** 75

---

## 🔴 Критичные дублирования

### 1. Синхронные и стриминговые агенты

**Проблема:** Дублирование методов между синхронными и стриминговыми версиями агентов.

**Затронутые методы:**
- `_build_planning_prompt()` - дублируется в `planner.py` и `streaming_planner.py`
- `_clean_plan()` - дублируется в `planner.py` и `streaming_planner.py`
- `_clean_test_code()` - дублируется в `test_generator.py` и `streaming_test_generator.py`
- `_calculate_base_scores()` - дублируется в `reflection.py` и `streaming_reflection.py`
- `_build_analysis_prompt()` - дублируется в 4 местах (reflection, streaming_reflection, debugger, streaming_debugger)
- `reset()` - дублируется в 6 стриминговых агентах

**Оценка дублирования:** ~70% кода дублируется между синхронными и стриминговыми версиями.

**Решение:** Использовать `BaseAgent` для общей логики (уже частично реализовано).

---

### 2. Методы очистки кода

**Проблема:** Метод `_clean_code()` дублируется в 10+ файлах агентов.

**Затронутые файлы:**
- `agents/coder.py`
- `agents/streaming_coder.py`
- `agents/incremental_coder.py`
- И другие...

**Оценка:** ~65-73 строки кода дублируются в каждом файле.

**Решение:** Вынести в `BaseAgent._clean_code()` (уже частично реализовано).

---

### 3. Построение промптов

**Проблема:** Методы `_build_*_prompt()` дублируются в синхронных и стриминговых агентах.

**Примеры:**
- `_build_code_generation_prompt()` - в `coder.py` и `streaming_coder.py`
- `_build_test_generation_prompt()` - в `test_generator.py` и `streaming_test_generator.py`
- `_build_analysis_prompt()` - в 4 местах

**Оценка:** ~200-300 строк дублирующегося кода.

**Решение:** Использовать общие методы в `BaseAgent` или `CoderPromptBuilder`.

---

## 🟡 Средние дублирования

### 4. Вспомогательные функции

**Проблема:** Похожие функции в разных модулях.

**Примеры:**
- `_detect_language()` - в `agents/intent.py` и `infrastructure/prompt_enhancer.py`
- `_is_greeting()` - в `agents/intent.py` и `backend/middleware/log_filter.py`
- `_extract_function_code()` - в `agents/incremental_coder.py` и `infrastructure/autonomous_improver/adapters/python_adapter.py`
- `get_context_for_llm()` - в `agents/conversation.py` и `backend/types.py`
- `cleanup()` - в `agents/conversation.py` и `infrastructure/memory_optimizer.py`

**Оценка:** ~150-200 строк дублирующегося кода.

**Решение:** Вынести в `utils/helpers.py` или соответствующие модули.

---

### 5. Тестовые фикстуры

**Проблема:** Метод `client` дублируется в 6+ тестовых файлах.

**Затронутые файлы:**
- `tests/test_backend_metrics_router.py`
- `tests/test_code_executor_security.py`
- `tests/test_backend_code_executor_router.py`
- И другие...

**Решение:** Вынести в `tests/conftest.py` как общую фикстуру.

---

### 6. Контекстные менеджеры

**Проблема:** Методы `__enter__` и `__exit__` дублируются в logging модулях.

**Затронутые файлы:**
- `infrastructure/logging/sink.py`
- `infrastructure/logging/manager.py`

**Решение:** Использовать общий базовый класс или декоратор.

---

## ⚠️ Легаси код

### Статистика по типам маркеров

| Тип маркера | Количество | Файлов |
|-------------|------------|--------|
| `legacy` | 54 | 10 |
| `deprecated` | 17 | 8 |
| `todo_remove` | 1 | 1 |
| `fixme_remove` | 1 | 1 |
| `decorator_deprecated` | 1 | 1 |
| `comment_unused` | 1 | 1 |

### Файлы с легаси кодом

1. **agents/intent.py** - legacy маркеры
2. **agents/reflection.py** - legacy маркеры
3. **agents/streaming_reflection.py** - legacy маркеры
4. **agents/streaming_critic.py** - legacy маркеры
5. **agents/debugger.py** - legacy маркеры
6. **agents/streaming_coder.py** - legacy маркеры
7. И другие...

### План удаления легаси кода

См. `DEPRECATION.md` для детального плана удаления устаревшего кода.

**Основные кандидаты на удаление:**
- Синхронные методы в стриминговых агентах (после стабилизации стриминга)
- Устаревшие методы с пометкой `@deprecated`
- Код с пометкой `TODO: remove` или `FIXME: remove`

---

## 📋 Сводная таблица

| Категория | Количество | Строк кода (приблизительно) | Приоритет |
|-----------|------------|----------------------------|-----------|
| Синхронные/стриминговые агенты | 6 пар | ~2000 | 🔴 Критично |
| Методы очистки кода | 10+ | ~650 | 🔴 Критично |
| Построение промптов | 15+ | ~300 | 🟡 Средний |
| Вспомогательные функции | 10+ | ~200 | 🟡 Средний |
| Тестовые фикстуры | 6+ | ~50 | 🟢 Низкий |
| Легаси код | 75 маркеров | ~500-1000 | 🟡 Средний |

**Общая оценка дублирующегося кода:** ~3700-4200 строк

---

## 🎯 Рекомендации по исправлению

### Фаза 1: Критичные дублирования (1-2 недели)

1. ✅ **Использовать BaseAgent** (частично реализовано)
   - Вынести `_clean_code()` в `BaseAgent`
   - Вынести общие методы построения промптов
   - Унифицировать инициализацию LLM

2. **Унифицировать методы очистки**
   - Создать `BaseAgent._clean_code()` с общей логикой
   - Мигрировать все агенты на использование общего метода

3. **Централизовать построение промптов**
   - Использовать `CoderPromptBuilder` для всех агентов
   - Вынести общие шаблоны промптов

### Фаза 2: Средние дублирования (1 неделя)

4. **Вынести вспомогательные функции**
   - Создать `utils/helpers.py` для общих функций
   - Мигрировать дублирующиеся функции

5. **Унифицировать тестовые фикстуры**
   - Вынести общие фикстуры в `tests/conftest.py`

### Фаза 3: Легаси код (по мере готовности)

6. **Удалить устаревший код**
   - Следовать плану в `DEPRECATION.md`
   - Удалить синхронные методы после стабилизации стриминга

---

## 🔧 Инструменты для анализа

### Скрипт анализа

```bash
python3 scripts/analyze_duplication.py
```

**Что проверяет:**
- Дублирующиеся функции (точные копии)
- Похожие функции (по имени)
- Легаси маркеры (deprecated, legacy, TODO, FIXME)

### Дополнительные проверки

```bash
# Поиск дублирующихся методов очистки
grep -r "def.*_clean_code" agents/

# Поиск дублирующихся промптов
grep -r "def.*_build.*prompt" agents/

# Поиск легаси маркеров
grep -r "DEPRECATED\|deprecated\|LEGACY\|legacy" --include="*.py" .
```

---

## 📝 История изменений

| Дата | Изменение |
|------|-----------|
| 2026-01-25 | Создан отчет на основе автоматического анализа |
| 2026-01-21 | Предыдущий аудит (см. `docs/archive/CODE_DUPLICATION_AUDIT.md`) |

---

## ✅ Прогресс исправления

- [x] Создан `BaseAgent` с общей логикой
- [x] Частично вынесена инициализация LLM
- [ ] Полностью унифицированы методы очистки кода
- [ ] Централизованы промпты
- [ ] Вынесены вспомогательные функции
- [ ] Удален легаси код

---

**Следующие шаги:**
1. Завершить миграцию на `BaseAgent._clean_code()`
2. Централизовать построение промптов
3. Вынести вспомогательные функции в `utils/helpers.py`
4. Удалить легаси код согласно `DEPRECATION.md`
