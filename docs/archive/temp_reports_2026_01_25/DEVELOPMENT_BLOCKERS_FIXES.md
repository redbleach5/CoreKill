# Исправления проблем, мешающих разработке и отладке

**Дата:** 2026-01-25  
**Статус:** В процессе

---

## ✅ Исправлено

### 1. Добавлено логирование в `except Exception` без логирования

**Исправлено:**
- `infrastructure/workflow_nodes.py` - добавлено логирование в `_is_streaming_enabled()`
- `backend/routers/code_executor.py` - добавлено логирование при удалении временных файлов (2 места)

**Было:**
```python
except Exception:
    pass  # Ошибка скрывается
```

**Стало:**
```python
except Exception as e:
    logger.debug(f"⚠️ Не удалось удалить файл: {e}")
```

---

### 2. Удалены print() из production кода

**Исправлено:**
- `agents/incremental_coder.py` - заменен print() в примере на logger.info()
- `infrastructure/local_llm.py` - удален print() из примера в docstring

**Было:**
```python
print(f"Функция {step.function_name}: {'✅' if step.tests_passed else '❌'}")
```

**Стало:**
```python
logger.info(f"Функция {step.function_name}: {'✅' if step.tests_passed else '❌'}")
```

---

## 📋 Оставшиеся проблемы

### Критичные (требуют исправления)

1. **except Exception без логирования** - ~95 мест осталось
   - `agents/streaming_*` - несколько мест
   - `backend/api.py` - 3 места
   - `backend/routers/agent.py` - несколько мест
   - И другие...

2. **print() в production коде** - ~10 мест (остальные в скриптах/документации)
   - `agents/test_generator.py` - в промптах (это нормально)
   - `infrastructure/prompt_enhancer.py` - в промптах (это нормально)
   - `utils/token_counter.py` - нужно проверить
   - `utils/env_config.py` - нужно проверить

### Средние (можно исправить позже)

3. **type: ignore без комментариев** - 38 мест
4. **Слишком общие except Exception** - 143 места (с логированием, но можно уточнить)
5. **Wildcard imports** - 18 файлов

---

## 🎯 Следующие шаги

1. **Продолжить исправление except Exception без логирования** (95 мест)
2. **Проверить оставшиеся print()** - заменить на logger где нужно
3. **Добавить комментарии к type: ignore** (38 мест)

---

## 📝 Рекомендации

### Для разработчиков

1. **Всегда логируйте ошибки:**
   ```python
   except Exception as e:
       logger.error(f"❌ Ошибка: {e}", error=e)
   ```

2. **Используйте logger вместо print():**
   ```python
   # ПЛОХО
   print(f"Debug: {value}")
   
   # ХОРОШО
   logger.debug(f"Debug: {value}")
   ```

3. **Уточняйте типы исключений:**
   ```python
   # ПЛОХО
   except Exception as e:
       ...
   
   # ХОРОШО
   except ValueError as e:
       logger.error(f"Неверное значение: {e}", error=e)
   except RuntimeError as e:
       logger.error(f"Ошибка выполнения: {e}", error=e)
   except Exception as e:
       logger.error(f"Неожиданная ошибка: {e}", error=e)
       raise
   ```

---

## 🔧 Инструменты

### Скрипт анализа

```bash
python3 scripts/analyze_development_blockers.py
```

**Что проверяет:**
- Слишком общие except блоки
- Отсутствие логирования ошибок
- print() в коде
- type: ignore без комментариев

---

## ✅ Прогресс

- [x] Исправлено 3 критичных места
- [ ] Осталось ~95 мест с except Exception без логирования
- [ ] Осталось ~10 print() в production коде
- [ ] Осталось 38 type: ignore без комментариев

**Общий прогресс:** ~3% исправлено
