# Проблемы, мешающие разработке и отладке

**Дата:** 2026-01-25  
**Статус:** Анализ завершен

---

## 📊 Общая статистика

- **Проанализировано файлов:** 136 Python файлов
- **Критичных проблем:** 100+ мест
- **Средних проблем:** 500+ мест

---

## 🔴 КРИТИЧНЫЕ ПРОБЛЕМЫ

### 1. Слишком общие `except Exception` без логирования

**Проблема:** 100 мест где `except Exception` не логирует ошибку, что скрывает баги.

**Статистика:**
- Всего `except Exception`: 243 места
- Без логирования: 100 мест (41%)
- С логированием: 143 места (59%)

**Затронутые файлы (топ-10):**
- `agents/streaming_coder.py` - 2 проблем
- `agents/streaming_reflection.py` - 1 проблем
- `agents/streaming_critic.py` - 1 проблем
- `agents/streaming_debugger.py` - 1 проблем
- `agents/streaming_test_generator.py` - 1 проблем
- `backend/api.py` - 3 проблем
- `backend/routers/code_executor.py` - 3 проблем
- `backend/routers/agent.py` - 3 проблем
- `backend/middleware/log_filter.py` - 2 проблем
- `infrastructure/workflow_nodes.py` - 1 проблем

**Почему это проблема:**
- Ошибки скрываются, невозможно отладить
- Нет информации о том, что пошло не так
- Усложняет поиск багов

**Пример проблемного кода:**
```python
# ПЛОХО - ошибка скрывается
try:
    result = some_operation()
except Exception:
    return None  # Что пошло не так? Неизвестно!

# ХОРОШО - ошибка логируется
try:
    result = some_operation()
except Exception as e:
    logger.error(f"❌ Ошибка в some_operation: {e}", error=e)
    return None
```

**Решение:** Добавить логирование во все `except Exception` блоки.

---

### 2. `print()` в production коде

**Проблема:** 428 вхождений `print()` в коде, которые не должны быть в production.

**Затронутые файлы:**
- `agents/test_generator.py` - 2 print()
- `agents/incremental_coder.py` - 1 print()
- `agents/streaming_test_generator.py` - 2 print()
- `infrastructure/local_llm.py` - 1 print()
- `infrastructure/prompt_enhancer.py` - 3 print()
- `utils/token_counter.py` - 2 print()
- `utils/env_config.py` - 1 print()
- И другие...

**Почему это проблема:**
- `print()` не проходит через систему логирования
- Нельзя контролировать уровень логирования
- Захламляет вывод
- Не сохраняется в логи

**Решение:** Заменить все `print()` на `logger.debug/info/warning/error()`.

---

### 3. `type: ignore` без комментариев

**Проблема:** 38 мест где используется `# type: ignore` без объяснения почему.

**Почему это проблема:**
- Скрывает реальные проблемы типизации
- Непонятно, почему игнорируется проверка типов
- Может скрывать баги

**Решение:** Добавить комментарии к каждому `# type: ignore`:
```python
# type: ignore[assignment]  # LangGraph требует Dict[str, Any], но мы знаем что это AgentState
```

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### 4. TODO/FIXME без контекста

**Проблема:** 10 TODO/FIXME без описания что нужно сделать.

**Примеры:**
- `# TODO` - что делать?
- `# FIXME` - что исправить?

**Решение:** Добавить контекст:
```python
# TODO: Вынести в BaseAgent после стабилизации стриминга
# FIXME: Обработать случай когда модель недоступна
```

---

### 5. Слишком общие `except Exception` с логированием

**Проблема:** 143 места где `except Exception` логируется, но все равно слишком общий.

**Почему это проблема:**
- Скрывает конкретные типы ошибок
- Усложняет обработку разных типов ошибок
- Может перехватывать системные исключения (KeyboardInterrupt, SystemExit)

**Решение:** Использовать конкретные типы исключений:
```python
# ПЛОХО
except Exception as e:
    logger.error(f"Ошибка: {e}", error=e)

# ХОРОШО
except ValueError as e:
    logger.error(f"Неверное значение: {e}", error=e)
except RuntimeError as e:
    logger.error(f"Ошибка выполнения: {e}", error=e)
except Exception as e:
    logger.error(f"Неожиданная ошибка: {e}", error=e)
    raise  # Перебрасываем неожиданные ошибки
```

---

### 6. Wildcard imports (`import *`)

**Проблема:** 18 файлов используют `from module import *`.

**Почему это проблема:**
- Непонятно что импортируется
- Может вызвать конфликты имен
- Усложняет отладку

**Решение:** Использовать явные импорты:
```python
# ПЛОХО
from module import *

# ХОРОШО
from module import function1, function2, Class1
```

---

## 🟢 НИЗКИЕ ПРОБЛЕМЫ

### 7. Незавершенный код

**Статистика:** 0 функций с `pass` (хорошо!)

**Но есть:** 211 вхождений `pass` и `...` в коде - нужно проверить контекст.

---

### 8. Сложные функции

**Статистика:** 0 функций > 50 строк (хорошо!)

**Но нужно проверить:** Функции с высокой цикломатической сложностью.

---

## 📋 ПЛАН ИСПРАВЛЕНИЯ

### Фаза 1: Критичные проблемы (1-2 дня)

1. **Добавить логирование в except Exception без логирования** (100 мест)
   - Приоритет: 🔴 КРИТИЧНО
   - Время: 4-6 часов

2. **Заменить print() на logger** (428 мест, но многие в скриптах)
   - Приоритет: 🔴 КРИТИЧНО (для production кода)
   - Время: 2-3 часа

3. **Добавить комментарии к type: ignore** (38 мест)
   - Приоритет: 🟡 СРЕДНИЙ
   - Время: 1 час

### Фаза 2: Средние проблемы (2-3 дня)

4. **Уточнить типы исключений** (143 места)
   - Приоритет: 🟡 СРЕДНИЙ
   - Время: 6-8 часов

5. **Исправить wildcard imports** (18 файлов)
   - Приоритет: 🟡 СРЕДНИЙ
   - Время: 2-3 часа

6. **Добавить контекст к TODO/FIXME** (10 мест)
   - Приоритет: 🟢 НИЗКИЙ
   - Время: 30 минут

---

## 🔧 ИНСТРУМЕНТЫ ДЛЯ АНАЛИЗА

### Скрипт анализа

```bash
python3 scripts/analyze_development_blockers.py
```

**Что проверяет:**
- Слишком общие except блоки
- Отсутствие логирования ошибок
- print() в коде
- type: ignore без комментариев
- Незавершенный код
- Сложные функции

---

## ✅ ПРИОРИТЕТЫ

1. **Срочно исправить:**
   - `except Exception` без логирования (100 мест)
   - `print()` в production коде (не в скриптах)

2. **Исправить в ближайшее время:**
   - Уточнить типы исключений
   - Добавить комментарии к type: ignore

3. **Исправить когда будет время:**
   - Wildcard imports
   - TODO/FIXME без контекста

---

## 📝 ПРИМЕРЫ ИСПРАВЛЕНИЙ

### Пример 1: Добавить логирование

```python
# БЫЛО
try:
    result = operation()
except Exception:
    return None

# СТАЛО
try:
    result = operation()
except Exception as e:
    logger.error(f"❌ Ошибка в operation: {e}", error=e)
    return None
```

### Пример 2: Заменить print()

```python
# БЫЛО
print(f"Debug: {value}")

# СТАЛО
logger.debug(f"Debug: {value}")
```

### Пример 3: Уточнить тип исключения

```python
# БЫЛО
except Exception as e:
    logger.error(f"Ошибка: {e}", error=e)

# СТАЛО
except ValueError as e:
    logger.error(f"Неверное значение: {e}", error=e)
except RuntimeError as e:
    logger.error(f"Ошибка выполнения: {e}", error=e)
except Exception as e:
    logger.error(f"Неожиданная ошибка: {e}", error=e)
    raise
```

---

## 🎯 ИТОГ

**Основные проблемы:**
- 🔴 100 мест где ошибки скрываются без логирования
- 🔴 428 print() в коде (многие в скриптах, но есть и в production)
- 🟡 38 type: ignore без комментариев
- 🟡 143 слишком общих except Exception

**Время на исправление:** 2-3 дня

**Приоритет:** 🔴 КРИТИЧНО - эти проблемы серьезно мешают отладке и разработке.
