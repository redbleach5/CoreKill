# 🚀 Быстрый старт теста Autonomous Improver

## Шаг 1: Настройка для слабого железа

Отредактируйте `config.toml`:

```toml
[autonomous_improver]
enabled = true
max_files_per_cycle = 5      # Меньше файлов за раз
cycle_interval = 600         # 10 минут между циклами
min_confidence = 1.0         # Только 100% уверенность
```

## Шаг 2: Проверка модели

```bash
ollama list
# Если нет легкой модели:
ollama pull phi3:mini
```

## Шаг 3: Запуск теста (4 часа)

### Вариант 1: Через скрипт (рекомендуется)

```bash
cd /Users/ruslan/Downloads/testmatuscrsr
./scripts/RUN_TEST.sh 4.0
```

### Вариант 2: Напрямую

```bash
cd /Users/ruslan/Downloads/testmatuscrsr
python3 scripts/test_autonomous_improver.py --duration 4.0
```

Тест будет:
- ✅ Работать 4 часа в фоне
- ✅ Анализировать проект каждые 10 минут
- ✅ Находить улучшения с 100% уверенностью
- ✅ Выводить прогресс каждую минуту
- ✅ Сохранять результаты в `test_improver_results.json`

## Шаг 4: Анализ результатов

После завершения:

```bash
python3 scripts/analyze_improver_results.py test_improver_results.json
```

## Что ожидать

За 4 часа на слабом железе:
- 📁 Проанализировано: ~20-40 файлов (зависит от размера проекта)
- 💡 Найдено предложений: 5-15 (только с 100% уверенностью)
- ⏱️ Время анализа: 30-60 секунд на файл

## Мониторинг

Тест выводит прогресс:
```
⏱️ Прогресс: 1.5ч / 4.0ч (осталось: 2.5ч) | Предложений: 8
💡 Найдено 2 новых предложений
```

## Остановка теста

Нажмите `Ctrl+C` для корректной остановки.

## Результаты

После теста проверьте:
1. `test_improver_results.json` - все предложения
2. Вывод `analyze_improver_results.py` - статистика и ТОП предложений
3. Логи - для отладки проблем

## Критерии успеха

✅ Найдено хотя бы 5-10 предложений  
✅ Хотя бы 30% с приоритетом >= 7  
✅ Хотя бы 50% с уверенностью >= 0.9  
✅ Предложения распределены по типам  
✅ Нет очевидно неправильных предложений
