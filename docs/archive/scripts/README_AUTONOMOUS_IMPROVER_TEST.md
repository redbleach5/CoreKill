# Тестирование Autonomous Improver

Руководство по тестированию модуля автономного улучшения проекта на слабом железе.

## Подготовка

### 1. Настройка для слабого железа

Отредактируйте `config.toml`:

```toml
[autonomous_improver]
enabled = true
project_path = ""  # Текущая директория
model = ""  # Автовыбор легкой модели
min_confidence = 1.0  # Только 100% уверенность
max_files_per_cycle = 5  # Меньше файлов за раз (для слабого железа)
cycle_interval = 600  # 10 минут между циклами (реже для экономии ресурсов)
```

### 2. Проверка доступности моделей

Убедитесь, что установлена легкая модель:

```bash
ollama list
# Должна быть хотя бы одна легкая модель: phi3:mini, gemma:2b, tinyllama:1.1b
```

Если нет, установите:

```bash
ollama pull phi3:mini  # Самая легкая модель
```

## Запуск теста

### Базовый тест (4 часа)

```bash
cd /Users/ruslan/Downloads/testmatuscrsr
python scripts/test_autonomous_improver.py --duration 4.0
```

### Кастомный тест

```bash
# Тест на 2 часа
python scripts/test_autonomous_improver.py --duration 2.0

# Тест на 8 часов
python scripts/test_autonomous_improver.py --duration 8.0

# Сохранить результаты в другой файл
python scripts/test_autonomous_improver.py --duration 4.0 --output my_results.json
```

## Мониторинг во время теста

Тест выводит прогресс каждую минуту:

```
⏱️ Прогресс: 1.5ч / 4.0ч (осталось: 2.5ч) | Предложений: 12
💡 Найдено 3 новых предложений
```

## Анализ результатов

После завершения теста:

```bash
python scripts/analyze_improver_results.py test_improver_results.json
```

Скрипт покажет:
- Общую статистику
- Распределение по типам улучшений
- Распределение по приоритетам
- ТОП файлов с предложениями
- Лучшие предложения

## Оценка качества предложений

### Критерии хорошего предложения:

1. **Высокий приоритет (>=7)** - действительно важное улучшение
2. **Высокая уверенность (>=0.9)** - модель уверена в предложении
3. **Чёткое обоснование** - есть reasoning почему это улучшение
4. **Конкретное предложение** - не абстрактное, а конкретное действие
5. **Пример кода** - если применимо, есть code_example

### Проверка вручную:

1. Откройте `test_improver_results.json`
2. Найдите предложения с `priority >= 7` и `confidence >= 0.9`
3. Проверьте каждое предложение:
   - Действительно ли это улучшение?
   - Правильно ли указана проблема?
   - Реализуемо ли предложение?
   - Не сломает ли это существующий код?

## Метрики успешности

Тест считается успешным, если:

- ✅ Найдено хотя бы 5-10 предложений за 4 часа
- ✅ Хотя бы 30% предложений имеют приоритет >= 7
- ✅ Хотя бы 50% предложений имеют уверенность >= 0.9
- ✅ Предложения распределены по разным типам (не только один тип)
- ✅ Нет очевидно неправильных предложений

## Оптимизация для слабого железа

Если тест работает слишком медленно:

1. **Уменьшите количество файлов за цикл:**
   ```toml
   max_files_per_cycle = 3  # Вместо 5
   ```

2. **Увеличьте интервал между циклами:**
   ```toml
   cycle_interval = 900  # 15 минут вместо 10
   ```

3. **Используйте самую легкую модель:**
   ```toml
   model = "phi3:mini"  # Или "tinyllama:1.1b"
   ```

4. **Ограничьте область анализа:**
   ```toml
   project_path = "agents"  # Только папка agents
   ```

## Примеры хороших предложений

### Качество кода

```json
{
  "type": "code_quality",
  "file_path": "agents/coder.py",
  "description": "Функция generate_code имеет цикломатическую сложность 15",
  "suggestion": "Разбить функцию на несколько меньших функций",
  "confidence": 1.0,
  "priority": 8,
  "reasoning": "Высокая сложность затрудняет поддержку и тестирование",
  "estimated_impact": "medium"
}
```

### Производительность

```json
{
  "type": "performance",
  "file_path": "infrastructure/context_engine.py",
  "description": "Использование list comprehension вместо цикла for",
  "suggestion": "Заменить цикл for на list comprehension",
  "confidence": 1.0,
  "priority": 6,
  "reasoning": "List comprehension быстрее на 10-20%",
  "estimated_impact": "low"
}
```

## Устранение проблем

### Тест не находит предложений

- Проверьте, что `min_confidence = 1.0` (только 100% уверенность)
- Уменьшите до `0.9` для тестирования: `min_confidence = 0.9`
- Проверьте логи на наличие ошибок

### Тест работает слишком медленно

- Уменьшите `max_files_per_cycle`
- Увеличьте `cycle_interval`
- Используйте более легкую модель

### Много ошибок в логах

- Проверьте доступность Ollama: `ollama list`
- Проверьте доступность модели
- Уменьшите нагрузку (меньше файлов за цикл)

## Следующие шаги

После успешного теста:

1. **Проверьте предложения вручную** - откройте JSON и проверьте каждое
2. **Примените лучшие предложения** - начните с высокого приоритета
3. **Оцените результаты** - действительно ли улучшения помогли?
4. **Настройте параметры** - оптимизируйте для вашего железа
5. **Запустите на production** - включите `enabled = true` для постоянной работы
