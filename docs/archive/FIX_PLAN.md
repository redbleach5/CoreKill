# План исправления критических проблем

**Дата:** 2026-01-23  
**Статус:** В процессе

---

## 🎯 Приоритеты исправления

### Фаза 1: Критические проблемы безопасности (НЕМЕДЛЕННО)

#### ✅ Задача 1.1: Замена exec() на subprocess в validate_code_quick()

**Файл:** `utils/validation.py:351-424`

**Проблема:**
- Использование `exec()` для выполнения кода - критическая уязвимость безопасности
- Нет изоляции процесса
- Нет ограничений на ресурсы

**Решение:**
1. Заменить `exec()` на `subprocess.run()` (синхронная версия, т.к. функция синхронная)
2. Добавить таймаут (по умолчанию 5 секунд)
3. Ограничить размер вывода (stdout/stderr)
4. Использовать временные файлы для кода и тестов
5. Сохранить обратную совместимость API

**Шаги:**
- [x] Изучить текущую реализацию
- [ ] Создать безопасную функцию выполнения через subprocess
- [ ] Заменить exec() на subprocess
- [ ] Добавить таймауты и ограничения
- [ ] Обновить тесты
- [ ] Проверить обратную совместимость

---

### Фаза 2: Важные проблемы (В течение недели)

#### Задача 2.1: Исправление утечек памяти в ConfidenceAccumulator

**Файл:** `infrastructure/autonomous_improver/confidence_accumulator.py`

**Решение:**
- Добавить max_history_size
- Периодическая очистка старых записей
- LRU cache для истории

#### Задача 2.2: Улучшение обработки JSON парсинга

**Файл:** `infrastructure/autonomous_improver/core.py:1368`

**Решение:**
- Извлечение JSON из markdown блоков
- Попытка исправить распространенные ошибки
- Накопление статистики

---

### Фаза 3: Аудит и очистка (В течение месяца)

#### Задача 3.1: Аудит orphan модулей

**Решение:**
- Проверить динамические импорты
- Проверить использование через декораторы
- Документировать или удалить неиспользуемые модули

---

## 📋 Чеклист выполнения

- [ ] Фаза 1: Критические проблемы безопасности
  - [ ] Задача 1.1: Замена exec() на subprocess
- [ ] Фаза 2: Важные проблемы
  - [ ] Задача 2.1: Утечки памяти
  - [ ] Задача 2.2: JSON парсинг
- [ ] Фаза 3: Аудит и очистка
  - [ ] Задача 3.1: Orphan модули
