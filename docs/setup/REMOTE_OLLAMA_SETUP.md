# Настройка удалённого Ollama (RTX 3090)

Инструкция по использованию Ollama с ПК, оснащённого RTX 3090, с вашего MacBook.

## Шаг 1: Настройка Ollama на ПК с RTX 3090

### Вариант A: Локальная сеть (рекомендуется для домашней сети)

1. **Узнайте IP-адрес ПК в локальной сети:**
   ```bash
   # На ПК с Windows:
   ipconfig
   # Найдите IPv4 адрес (например, 192.168.1.100)
   
   # На ПК с Linux:
   ip addr show
   # или
   hostname -I
   ```

2. **Настройте Ollama для приёма удалённых подключений:**
   
   На ПК с RTX 3090 запустите Ollama с привязкой ко всем интерфейсам:
   
   ```bash
   # Windows (PowerShell или CMD):
   $env:OLLAMA_HOST="0.0.0.0:11434"
   ollama serve
   
   # Linux:
   OLLAMA_HOST=0.0.0.0:11434 ollama serve
   ```
   
   Или создайте файл `.env` в директории Ollama и добавьте:
   ```
   OLLAMA_HOST=0.0.0.0:11434
   ```

3. **Настройте файрвол на ПК:**
   
   Разрешите входящие подключения на порт 11434:
   
   - **Windows:** 
     - Откройте "Брандмауэр Защитника Windows"
     - "Дополнительные параметры" → "Правила для входящих подключений"
     - "Создать правило" → "Порт" → TCP → 11434 → Разрешить
   
   - **Linux:**
     ```bash
     sudo ufw allow 11434/tcp
     # или для firewalld:
     sudo firewall-cmd --add-port=11434/tcp --permanent
     sudo firewall-cmd --reload
     ```

### Вариант B: Через Tailscale (для разных сетей или VPN)

Если ПК и MacBook в разных сетях, используйте Tailscale:

1. **Установите Tailscale на обе машины:**
   - MacBook: https://tailscale.com/download/macos
   - ПК: https://tailscale.com/download/windows (или Linux)

2. **Подключите обе машины к одному Tailscale аккаунту**

3. **Узнайте Tailscale IP ПК:**
   ```bash
   # На ПК:
   tailscale ip
   # Вернёт что-то вроде: 100.x.x.x
   ```

4. **Настройте Ollama на ПК:**
   ```bash
   OLLAMA_HOST=0.0.0.0:11434 ollama serve
   ```

## Шаг 2: Настройка на MacBook

### Способ 1: Через config.toml (рекомендуется)

Откройте `config.toml` и обновите секцию `[ollama]`:

```toml
[ollama]
# Для локальной сети (замените на IP вашего ПК):
host = "http://192.168.1.100:11434"

# Или для Tailscale:
# host = "http://100.x.x.x:11434"

# Увеличьте таймауты для удалённого подключения:
connect_timeout = 30
timeout = 600

# Включите использование удалённого Ollama:
use_remote = true
```

### Способ 2: Через переменные окружения

Установите переменную окружения перед запуском:

```bash
# В терминале:
export OLLAMA_HOST="http://192.168.1.100:11434"
python run.py

# Или в .zshrc/.bashrc для постоянного использования:
echo 'export OLLAMA_HOST="http://192.168.1.100:11434"' >> ~/.zshrc
source ~/.zshrc
```

## Шаг 3: Проверка подключения

Используйте скрипт для проверки:

```bash
python scripts/test_remote_ollama.py
```

Или вручную:

```bash
# Проверка доступности:
curl http://192.168.1.100:11434/api/tags

# Проверка через Python:
python -c "import ollama; ollama.Client(host='http://192.168.1.100:11434').list()"
```

## Шаг 4: Проверка работы проекта

Запустите проект и проверьте логи:

```bash
python run.py
```

В логах должно появиться:
```
🌐 Ollama хост: http://192.168.1.100:11434
```

## Автоматический выбор моделей

**Важно:** Проект автоматически обнаруживает и использует любые модели, установленные на удалённом Ollama!

### Как это работает:

1. **Динамическое сканирование:** При старте проект сканирует все доступные модели через `ollama.list()`
2. **Умный выбор:** `SmartModelRouter` автоматически выбирает лучшую модель на основе:
   - Сложности задачи (SIMPLE/MEDIUM/COMPLEX)
   - Качества модели (оценка 0.0-1.0)
   - Специализации (coder, reasoning)
   - Доступной VRAM

3. **Нет жёсткой привязки:** Проект не требует конкретных моделей — использует любые доступные

### Примеры:

Если на ПК с RTX 3090 установлены:
- `llama3:70b` — будет использоваться для COMPLEX задач
- `qwen2.5-coder:32b` — будет использоваться для MEDIUM/COMPLEX задач
- `deepseek-r1:32b` — будет использоваться для COMPLEX задач (reasoning)
- `phi3:mini` — будет использоваться для SIMPLE задач (intent, planning)

Проект автоматически выберет оптимальную модель для каждой задачи!

### Проверка доступных моделей:

```bash
# С MacBook (через удалённый Ollama):
curl http://192.168.1.100:11434/api/tags

# Или через Python:
python -c "import ollama; client = ollama.Client(host='http://192.168.1.100:11434'); print(client.list())"
```

### Установка моделей на ПК:

```bash
# На ПК с RTX 3090:
ollama pull llama3:70b          # Для очень сложных задач
ollama pull qwen2.5-coder:32b   # Для генерации кода
ollama pull deepseek-r1:32b      # Reasoning модель
ollama pull phi3:mini            # Лёгкая модель для простых задач
```

Проект автоматически обнаружит их при следующем запуске!

## Устранение проблем

### Проблема: "Connection refused" или таймауты

1. **Проверьте, что Ollama запущен на ПК:**
   ```bash
   # На ПК:
   curl http://localhost:11434/api/tags
   ```

2. **Проверьте файрвол:**
   - Убедитесь, что порт 11434 открыт
   - Проверьте, что Ollama слушает на 0.0.0.0, а не только на localhost

3. **Проверьте сеть:**
   ```bash
   # С MacBook:
   ping 192.168.1.100
   telnet 192.168.1.100 11434
   ```

### Проблема: Медленные ответы

1. **Увеличьте таймауты в config.toml:**
   ```toml
   [ollama]
   timeout = 600  # 10 минут для больших моделей
   ```

2. **Проверьте загрузку GPU на ПК:**
   - Используйте `nvidia-smi` для мониторинга
   - Убедитесь, что модель загружена в VRAM

### Проблема: Модели не найдены

Модели должны быть установлены на ПК с RTX 3090, а не на MacBook:

```bash
# На ПК с RTX 3090:
ollama pull qwen2.5-coder:7b
ollama pull deepseek-r1:7b
# и т.д.
```

## Оптимизация производительности

1. **Используйте квантованные модели:**
   - `qwen2.5-coder:7b-q4_0` вместо `qwen2.5-coder:7b`
   - Меньше VRAM, быстрее инференс

2. **Настройте размер контекста:**
   ```toml
   [llm]
   tokens_code = 4096  # Для RTX 3090 можно больше
   ```

3. **Используйте connection pool:**
   ```toml
   [ollama]
   connection_pool_size = 20  # Больше для RTX 3090
   ```

## Безопасность

⚠️ **Важно:** Открытие Ollama для удалённого доступа делает его доступным для всех в вашей сети.

### Рекомендации:

1. **Используйте Tailscale** вместо открытия порта в интернет
2. **Ограничьте доступ по IP** в файрволе (только IP MacBook)
3. **Не открывайте порт 11434 в интернет** без дополнительной защиты

## Примеры конфигурации

### Локальная сеть (192.168.1.x)
```toml
[ollama]
host = "http://192.168.1.100:11434"
connect_timeout = 30
timeout = 600
use_remote = true
```

### Tailscale VPN
```toml
[ollama]
host = "http://100.64.1.5:11434"
connect_timeout = 30
timeout = 600
use_remote = true
```

### Переключение между локальным и удалённым
```toml
[ollama]
host = "http://192.168.1.100:11434"
use_remote = false  # false = использовать локальный, true = удалённый
```
