# Аудит масштабируемости тестов

**Дата:** 2026-01-21  
**Статус:** 🔍 Обнаружены проблемы

---

## 🔴 Критические проблемы масштабируемости

### 1. Дублирование моков в frontend тестах

**Проблема:** `localStorageMock` определен в нескольких местах:
- `frontend/src/__tests__/setup.ts`
- `frontend/src/__tests__/hooks.test.tsx`

**Последствия:**
- При изменении логики localStorage нужно обновлять несколько мест
- Риск рассинхронизации моков
- Сложнее поддерживать

**Решение:** Вынести в общий test utility

---

### 2. Дублирование логики создания моков агентов

**Проблема:** Каждый тест агента создает свои моки:
- `test_coder.py` — `mock_coder_dependencies()`
- `test_planner.py` — локальные моки в fixture
- `test_reflection.py` — локальные моки в fixture

**Последствия:**
- При изменении структуры агентов нужно обновлять много мест
- Дублирование кода
- Сложнее добавлять новые тесты

**Решение:** Создать общие fixtures в `conftest.py`

---

### 3. Отсутствие test factories

**Проблема:** Нет централизованных функций для создания тестовых данных:
- `AgentState` создается вручную в каждом тесте
- Нет factory для `IntentResult`, `ReflectionResult`, etc.

**Последствия:**
- При изменении структуры данных нужно обновлять все тесты
- Сложно создавать вариации тестовых данных
- Дублирование кода

**Решение:** Создать test factories

---

### 4. Отсутствие общих test utilities для frontend

**Проблема:** Нет общих утилит для:
- Создания моков API ответов
- Создания тестовых компонентов
- Создания тестовых хуков

**Последствия:**
- Дублирование кода в тестах
- Сложнее писать новые тесты
- Нет единого стиля тестирования

**Решение:** Создать `frontend/src/__tests__/utils/`

---

### 5. Хардкод значений в тестах

**Проблема:** Много магических значений:
- `"test-123"`, `"test-model"`, `"codellama:7b"`
- Хардкод URL, портов, таймаутов

**Последствия:**
- Сложно менять конфигурацию
- Непонятно откуда берутся значения
- Риск ошибок при копировании

**Решение:** Использовать константы и конфигурацию

---

### 6. Отсутствие параметризованных тестов

**Проблема:** Много похожих тестов с разными данными:
- Тесты для разных моделей
- Тесты для разных типов задач
- Тесты для разных ошибок

**Последствия:**
- Дублирование кода
- Сложно добавлять новые случаи
- Сложнее поддерживать

**Решение:** Использовать `@pytest.mark.parametrize`

---

## 🟡 Средние проблемы

### 7. Нет общих helpers для async тестов

**Проблема:** Каждый тест по-своему обрабатывает async операции

**Решение:** Создать общие helpers

---

### 8. Нет общих моков для внешних зависимостей

**Проблема:** Ollama, ChromaDB мокаются в каждом тесте отдельно

**Решение:** Создать общие fixtures

---

## ✅ Что уже хорошо

1. ✅ Есть `conftest.py` с базовыми fixtures
2. ✅ Есть `setup.ts` для frontend тестов
3. ✅ Есть маркеры для категоризации тестов
4. ✅ Есть структура тестов (классы, describe блоки)

---

## 📋 План улучшений

### Приоритет 1: Критично (блокирует масштабируемость)

1. **Создать общие test utilities для frontend**
   - `frontend/src/__tests__/utils/mocks.ts` — общие моки
   - `frontend/src/__tests__/utils/testHelpers.tsx` — helpers для тестов

2. **Создать общие fixtures для backend**
   - Расширить `conftest.py` с общими fixtures для агентов
   - Создать test factories для тестовых данных

3. **Создать test factories**
   - `tests/factories.py` — factories для создания тестовых данных
   - `frontend/src/__tests__/utils/factories.ts` — factories для frontend

### Приоритет 2: Важно (улучшает поддерживаемость)

4. **Вынести константы**
   - `tests/constants.py` — константы для тестов
   - `frontend/src/__tests__/utils/constants.ts` — константы для frontend тестов

5. **Добавить параметризованные тесты**
   - Использовать `@pytest.mark.parametrize` для похожих тестов
   - Использовать `test.each` для frontend

6. **Создать общие helpers для async**
   - Helpers для ожидания async операций
   - Helpers для моков async функций

---

**Статус:** Требуется рефакторинг для масштабируемости
