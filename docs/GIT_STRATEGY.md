# Стратегия работы с Git после масштабного рефакторинга

**Дата:** 2026-01-21  
**Статус:** ✅ Рекомендация

---

## 🎯 Рекомендация: **Коммитить в существующий репозиторий**

### ✅ Почему НЕ создавать новый репозиторий:

1. **Проект активный и развивается**
   - Есть история коммитов и контекст
   - Репозиторий уже настроен и подключен
   - Есть документация и roadmap

2. **Это рефакторинг, а не переписка**
   - API совместим (backend/frontend интерфейсы не изменились)
   - Функциональность сохранена
   - Улучшена структура, но не меняется суть

3. **Сохраняется история проекта**
   - Важно видеть эволюцию кода
   - Легче откатиться при необходимости
   - История помогает понять изменения

4. **Удобство для команды/пользователей**
   - Не нужно менять URL репозитория
   - Не нужно обновлять документацию с новыми ссылками
   - Сохраняется continuity

---

## 📋 План действий

### Вариант 1: Большой коммит в main (рекомендуется для личных проектов)

**Подходит если:**
- Проект личный/приватный
- Нет активных PR от других разработчиков
- Хотите сохранить простоту

**Шаги:**

```bash
# 1. Создать тег для версии до рефакторинга (опционально, но рекомендуется)
git tag -a v0.9.0-pre-refactor -m "Версия до масштабного рефакторинга"

# 2. Обновить CHANGELOG
# (уже обновлен в docs/CHANGELOG.md)

# 3. Подготовить коммит
git add .

# 4. Создать большой коммит с детальным сообщением
git commit -m "refactor: масштабный рефакторинг для масштабируемости

BREAKING CHANGES: Нет (API совместим)

Основные изменения:
- Backend: устранено ~1500 строк дублирующегося кода
- Frontend: созданы централизованные utilities и hooks
- Тесты: добавлена инфраструктура для масштабируемости
- Документация: реорганизована и оптимизирована

Детали:
- Создан apiClient.ts для централизованных API вызовов
- Созданы хуки: useApi, useModels, useLocalStorage
- Созданы factories и fixtures для тестов
- Устранено дублирование в 25+ файлах
- Улучшена поддерживаемость и читаемость кода

Файлы:
- 62 файла изменено
- +4907 строк добавлено
- -2889 строк удалено
- Чистое улучшение: +2018 строк

Документация:
- docs/REFACTORING_HISTORY.md - полная история рефакторинга
- docs/TEST_SCALABILITY_FINAL_REPORT.md - улучшения тестов
- docs/CHANGELOG.md - обновлен

Тесты:
- Добавлены frontend тесты (vitest)
- Улучшена инфраструктура backend тестов
- Созданы общие utilities и factories

Совместимость:
- API endpoints не изменились
- Frontend компоненты работают без изменений
- Конфигурация совместима"

# 5. Отправить в репозиторий
git push origin main

# 6. Отправить тег (если создали)
git push origin v0.9.0-pre-refactor
```

---

### Вариант 2: Отдельная ветка + Merge (рекомендуется для публичных проектов)

**Подходит если:**
- Проект публичный
- Есть другие разработчики
- Хотите review перед merge

**Шаги:**

```bash
# 1. Создать ветку для рефакторинга
git checkout -b refactor/massive-refactoring-2026-01-21

# 2. Создать тег для текущей версии main
git checkout main
git tag -a v0.9.0-pre-refactor -m "Версия до масштабного рефакторинга"
git checkout refactor/massive-refactoring-2026-01-21

# 3. Подготовить коммит (то же сообщение, что в Варианте 1)
git add .
git commit -m "refactor: масштабный рефакторинг для масштабируемости
[... то же сообщение ...]"

# 4. Отправить ветку
git push origin refactor/massive-refactoring-2026-01-21

# 5. Создать Pull Request на GitHub
# (через веб-интерфейс или GitHub CLI)

# 6. После review и approval - merge в main
git checkout main
git merge refactor/massive-refactoring-2026-01-21
git push origin main
```

---

## 📝 Структура commit message

Рекомендуемый формат:

```
refactor: масштабный рефакторинг для масштабируемости

BREAKING CHANGES: Нет (API совместим)

Основные изменения:
- [Категория 1]: краткое описание
- [Категория 2]: краткое описание

Детали:
- [Конкретное изменение 1]
- [Конкретное изменение 2]

Файлы:
- X файлов изменено
- +Y строк добавлено
- -Z строк удалено

Документация:
- [Список обновленных документов]

Тесты:
- [Что добавлено/улучшено]

Совместимость:
- [Что совместимо, что нет]
```

---

## 🏷️ Версионирование

### Рекомендуемая схема:

**До рефакторинга:**
- `v0.9.0` или `v0.9.0-pre-refactor` (тег)

**После рефакторинга:**
- `v0.10.0` или `v1.0.0` (если считаете это major milestone)

**Обоснование:**
- Рефакторинг не ломает API → minor версия (0.9 → 0.10)
- Если считаете это major milestone → major версия (0.9 → 1.0)

---

## ⚠️ Что НЕ делать

1. ❌ **НЕ коммитить `.chromadb_test/chroma.sqlite3`**
   - Это база данных, должна быть в `.gitignore`
   - Проверьте `.gitignore` перед коммитом

2. ❌ **НЕ коммитить временные файлы**
   - `.cursor/mcp.json` (уже в `.gitignore`)
   - Логи, кэши, временные файлы

3. ❌ **НЕ делать force push в main**
   - Если нужно исправить - делайте новый коммит
   - Force push только в feature ветки

4. ❌ **НЕ забывать обновить документацию**
   - `README.md` - если изменилась установка
   - `CHANGELOG.md` - обязательно
   - `DOCS_INDEX.md` - если изменилась структура

---

## ✅ Чеклист перед коммитом

- [ ] Проверить `.gitignore` (не коммитить лишнее)
- [ ] Обновить `CHANGELOG.md`
- [ ] Обновить `README.md` (если нужно)
- [ ] Проверить, что все тесты проходят
- [ ] Проверить линтер (нет ошибок)
- [ ] Создать тег для версии до рефакторинга (опционально)
- [ ] Написать детальное commit message
- [ ] Проверить, что проект запускается после изменений

---

## 🔄 Если нужно откатиться

### Откат к версии до рефакторинга:

```bash
# Если создали тег
git checkout v0.9.0-pre-refactor

# Или по хешу коммита
git checkout <commit-hash-before-refactor>

# Создать ветку для восстановления
git checkout -b restore/pre-refactor-version
```

---

## 📊 Статистика изменений

**Текущие изменения:**
- 62 файла изменено
- +4907 строк добавлено
- -2889 строк удалено
- **Чистое улучшение: +2018 строк**

**Категории:**
- Backend рефакторинг: ~1500 строк дублирования устранено
- Frontend рефакторинг: ~800 строк дублирования устранено
- Тесты: +500 строк (новая инфраструктура)
- Документация: реорганизована

---

## 🎯 Итоговая рекомендация

**✅ Коммитить в существующий репозиторий `CoreKill`**

**Способ:**
- Для личного проекта: **Вариант 1** (большой коммит в main)
- Для публичного проекта: **Вариант 2** (ветка + PR)

**Версионирование:**
- Создать тег `v0.9.0-pre-refactor` перед коммитом
- После коммита: `v0.10.0` (minor) или `v1.0.0` (major milestone)

**Commit message:**
- Использовать детальное сообщение (см. пример выше)
- Указать BREAKING CHANGES (если есть)
- Перечислить основные изменения

---

**Последнее обновление:** 2026-01-21  
**Статус:** ✅ Готово к коммиту
