# Autonomous Improver - Автономное улучшение проекта

Модуль для фонового анализа кодовой базы и предложения аргументированных улучшений при 100% уверенности.

**Версия:** v2-universal (Этап 2 завершён)  
**Поддержка:** Python + Frontend (JS/TS/TSX/HTML/MD/JSON)

## Особенности

- **Работает в фоне** - не блокирует основной процесс
- **Только при 100% уверенности** - предлагает улучшения только когда полностью уверен
- **Не применяет автоматически** - только предлагает пользователю
- **Умная логика** - `ConfidenceAccumulator` накапливает уверенность через повторения, стабильность, AST/структурное подтверждение
- **Экономия ресурсов** - использует легкие модели, работает в свободное время
- **Работает на слабом железе** - оптимизирован для ограниченных ресурсов
- **⚡ Параллельный анализ** - анализирует несколько файлов одновременно (3-5x ускорение)
- **💾 Кэширование** - не анализирует повторно неизменённые файлы (50-80% экономия)
- **🎯 Приоритизация** - анализирует сначала файлы с наибольшим потенциалом улучшений
- **🔍 Веб-поиск** - автоматически ищет лучшие практики при отсутствии прогресса
- **✅ Валидация** - фильтрует дубликаты и невалидные предложения
- **🧠 Умная выборка кода** - анализирует важные участки больших файлов (до 5000 символов)
- **📊 Метрики** - отслеживание эффективности работы модуля
- **🔄 Rotation предложений** - ограничение памяти (максимум 200 предложений)
- **🌐 Мультиязычность** - поддержка Python и Frontend (JS/TS/TSX/HTML/MD/JSON) через LanguageAdapter
- **🔧 Конфигурируемость** - `ProjectProfile` для настройки под разные типы проектов
- **🎨 Frontend-анализ** - обнаружение проблем accessibility, UX, типизации, структуры компонентов

## Конфигурация

```toml
[autonomous_improver]
# Включить автономное улучшение проекта
enabled = false

# Путь к проекту для анализа (None = текущая директория)
project_path = ""

# Модель для анализа (None = автовыбор легкой модели)
model = ""

# Минимальная уверенность для предложения (0.0-1.0)
# 1.0 = предлагать только при 100% уверенности
min_confidence = 1.0

# Максимум файлов для анализа за один цикл
max_files_per_cycle = 10

# Интервал между циклами анализа (секунды)
# 300 = 5 минут, 600 = 10 минут
cycle_interval = 300

# Максимальное количество файлов для параллельного анализа
# 3-5 оптимально для баланса скорости и нагрузки на LLM
max_parallel = 3
```

## Как это работает

1. **Фоновая задача** запускается при старте приложения (если `enabled = true`)
2. **Циклы анализа** - каждые N секунд (настраивается) анализирует проект
3. **Приоритизация файлов** - файлы сортируются по потенциалу улучшений (сложность, размер, отсутствие docstrings)
4. **Кэширование** - пропускаются файлы, которые не изменились с последнего анализа
5. **Параллельный анализ** - несколько файлов анализируются одновременно (3-5 файлов)
6. **Структурный анализ** - использует AST для понимания структуры кода
7. **LLM анализ** - легкая модель анализирует код и ищет улучшения
8. **Веб-поиск** - при отсутствии прогресса автоматически ищет лучшие практики
9. **Накопление уверенности** - предложения накапливаются, фильтруются по уверенности
10. **Предложения пользователю** - только при 100% уверенности (настраивается)

## Оптимизации производительности

### ⚡ Параллельный анализ
- Анализирует 3-5 файлов одновременно (настраивается через `max_parallel`)
- **Эффект:** 3-5x ускорение анализа

### 💾 Кэширование результатов
- Хранит хеш содержимого файла при анализе
- Пропускает файлы, которые не изменились
- Кэш действителен 24 часа
- **Эффект:** 50-80% экономии времени на повторных анализах

### 🎯 Приоритизация файлов
- Анализирует сначала файлы с наибольшим потенциалом:
  - Высокая сложность функций (> 10)
  - Много функций (> 20)
  - Большой размер (> 500 строк)
  - Отсутствие docstrings
- **Эффект:** 2-3x больше полезных предложений

### ✅ Валидация предложений
- Фильтрует дубликаты по хешу
- Проверяет минимальную длину описания (20+ символов)
- Проверяет наличие конкретного предложения (30+ символов)
- **Эффект:** Только качественные, уникальные предложения

### 🧠 Умная выборка кода
- Для больших файлов (> 5000 символов) выбирает важные участки:
  - Начало файла (импорты, константы, классы)
  - Сложные функции (complexity > 10)
  - Конец файла (основная логика)
- **Эффект:** Лучший контекст при анализе больших файлов

### 📊 Метрики эффективности
- Отслеживает количество проанализированных файлов
- Статистика по типам и приоритетам предложений
- Средняя уверенность предложений
- Распределение по типам улучшений
- **Использование:** `improver.get_metrics()`

## API

### Получить метрики

```http
GET /api/improvements/metrics
```

**Ответ:**
```json
{
  "total_files_analyzed": 45,
  "total_suggestions": 12,
  "high_confidence_suggestions": 8,
  "average_confidence": 0.92,
  "cycles_without_progress": 0,
  "suggestion_types": {
    "code_quality": 5,
    "performance": 3,
    "documentation": 4
  },
  "priority_distribution": {
    "8": 3,
    "7": 5,
    "6": 4
  }
}
```

**Или через Python:**
```python
from infrastructure.autonomous_improver import get_autonomous_improver

improver = get_autonomous_improver()
metrics = improver.get_metrics()
print(f"Проанализировано файлов: {metrics['total_files_analyzed']}")
```

### Получить предложения

```http
GET /api/improvements?min_confidence=1.0
```

**Ответ:**
```json
{
  "suggestions": [
    {
      "type": "code_quality",
      "file_path": "agents/coder.py",
      "description": "Функция слишком сложная",
      "suggestion": "Разбить на несколько функций",
      "confidence": 1.0,
      "priority": 8,
      "reasoning": "Цикломатическая сложность 15 превышает рекомендуемый порог 10",
      "estimated_impact": "medium",
      "code_example": "...",
      "metadata": {}
    }
  ],
  "count": 1,
  "min_confidence": 1.0
}
```

### Очистить предложения

```http
POST /api/improvements/clear
```

## Типы улучшений

### Общие типы
- `code_quality` - Улучшение качества кода (читаемость, поддерживаемость)
- `performance` - Оптимизация производительности
- `security` - Улучшение безопасности
- `documentation` - Добавление документации
- `refactoring` - Рефакторинг
- `architecture` - Архитектурные улучшения

### Фронтенд-специфичные типы
- `accessibility` - Доступность (a11y) для UI/UX кода
- `ux` - User experience улучшения
- `types` - TypeScript типизация и типы
- `component_design` - Структура компонентов

## Приоритеты

- 1-3: Низкий приоритет (можно отложить)
- 4-6: Средний приоритет (стоит рассмотреть)
- 7-9: Высокий приоритет (рекомендуется исправить)
- 10: Критично (требует немедленного внимания)

## Оценка влияния

- `low` - Низкое влияние (косметические улучшения)
- `medium` - Среднее влияние (улучшение качества/производительности)
- `high` - Высокое влияние (критические улучшения)

## Тестирование

### Простой запуск (рекомендуется)

```bash
# Запуск теста на 4 часа (по умолчанию)
./run_improver.sh

# Или с кастомной длительностью
./run_improver.sh 2.0  # 2 часа
```

### Альтернативный запуск

```bash
# Тест на 4 часа (напрямую через скрипт)
python3 infrastructure/autonomous_improver/scripts/test.py --duration 4.0

# Анализ результатов
python3 infrastructure/autonomous_improver/scripts/analyze_results.py test_improver_results.json
```

**Быстрый старт:** [scripts/START_TEST.md](../scripts/START_TEST.md)

---

## 📜 Скрипты

### `scripts/test.py`

**Назначение:** Тестовый скрипт для проверки Autonomous Improver

**Использование:**
```bash
python3 infrastructure/autonomous_improver/scripts/test.py --duration 4.0
```

**Параметры:**
- `--duration`: Длительность теста в часах (по умолчанию 4.0)

**Результаты:**
- Логи модуля: `logs/autonomous_improver.log`
- Логи теста: `logs/autonomous_improver_test.log`
- Результаты: `test_improver_results.json`

---

### `scripts/analyze_results.py`

**Назначение:** Анализ результатов теста Autonomous Improver

**Использование:**
```bash
python3 infrastructure/autonomous_improver/scripts/analyze_results.py test_improver_results.json
```

**Выводит:**
- Общую статистику (количество предложений, средняя уверенность)
- Распределение по типам улучшений
- Распределение по приоритетам
- ТОП-10 файлов с наибольшим количеством предложений
- ТОП-5 лучших предложений (приоритет + уверенность)

---

### `scripts/run.sh`

**Назначение:** Простой shell скрипт для запуска Autonomous Improver

**Использование:**
```bash
./infrastructure/autonomous_improver/scripts/run.sh 4.0
```

**Что делает:**
1. Проверяет конфигурацию (`autonomous_improver.enabled = true`)
2. Создаёт директорию для логов
3. Запускает тест через `test.py`
4. Выводит информацию о результатах

**См. также:** [Shell скрипты проекта](../../scripts/SHELL_SCRIPTS.md)

## Логирование

Все логи модуля пишутся в:
- **Модуль:** `logs/autonomous_improver.log` (JSONL формат)
- **Тест:** `logs/autonomous_improver_test.log` (JSONL формат)

Просмотр логов:
```bash
# Логи модуля
tail -f logs/autonomous_improver.log

# Логи теста
tail -f logs/autonomous_improver_test.log
```

## Использование

### Программное использование

```python
from infrastructure.autonomous_improver import get_autonomous_improver

# Получить экземпляр
improver = get_autonomous_improver()

# Запустить (обычно делается автоматически при старте)
improver.start()

# Получить предложения
suggestions = improver.get_suggestions(min_confidence=1.0)

for suggestion in suggestions:
    print(f"{suggestion.file_path}: {suggestion.description}")
    print(f"  Предложение: {suggestion.suggestion}")
    print(f"  Уверенность: {suggestion.confidence}")
    print(f"  Приоритет: {suggestion.priority}")

# Остановить
improver.stop()
```

### Автоматический запуск

Модуль автоматически запускается при старте FastAPI приложения, если `autonomous_improver.enabled = true` в config.toml.

## Логика принятия решений

1. **Структурный анализ** - AST для Python, regex/парсинг для фронтенда (компоненты, хуки, типы)
2. **LLM анализ** - семантический анализ (качество, безопасность, архитектура, accessibility)
3. **Накопление уверенности** - `ConfidenceAccumulator` накапливает уверенность через повторения, стабильность кода, AST/структурное подтверждение, веб-подтверждение
4. **Проверка уверенности** - только предложения с эффективной уверенностью >= min_confidence отправляются пользователю
5. **Веб-поиск** - автоматический поиск best practices при отсутствии прогресса

## Поддерживаемые языки

### Python
- Анализ `.py` файлов
- AST-анализ структуры кода
- PEP 8 соответствие
- Docstrings и документация

### Frontend (JS/TS/TSX/HTML/MD/JSON)
- **TypeScript/JavaScript** (`.ts`, `.tsx`, `.js`) - типизация, компоненты, хуки
- **HTML** (`.html`) - семантическая разметка, accessibility
- **Markdown** (`.md`) - структура документации
- **JSON** (`.json`) - валидность, структура конфигурации

### Mixed проекты
- Автоматическое определение типа проекта
- Использование `MultiAdapter` для анализа нескольких языков одновременно
- Поддержка fullstack проектов (Python + TypeScript)

## Ограничения

- Исключает виртуальные окружения (`.venv`, `__pycache__`, `.git`, `node_modules`)
- Максимум файлов за цикл настраивается (по умолчанию 10)
- Использует легкие модели для экономии ресурсов
- Работает только в свободное время (не блокирует основной процесс)

## Рекомендации

### Для слабого железа

```toml
[autonomous_improver]
enabled = true
max_files_per_cycle = 5  # Меньше файлов за раз
cycle_interval = 600  # Реже (10 минут)
model = "phi3:mini"  # Самая легкая модель
```

### Для мощного железа

```toml
[autonomous_improver]
enabled = true
max_files_per_cycle = 20  # Больше файлов за раз
cycle_interval = 180  # Чаще (3 минуты)
model = ""  # Автовыбор оптимальной модели
```

## Безопасность

- **НЕ применяет изменения автоматически** - только предлагает
- **Только при 100% уверенности** - минимизирует ложные срабатывания
- **Проверка кода** - использует AST для точного анализа
- **Фильтрация** - исключает системные директории

## Примеры предложений

### Качество кода

```json
{
  "type": "code_quality",
  "file_path": "agents/coder.py",
  "description": "Функция generate_code имеет цикломатическую сложность 12",
  "suggestion": "Разбить функцию на несколько меньших функций",
  "confidence": 1.0,
  "priority": 7,
  "reasoning": "Высокая сложность затрудняет поддержку и тестирование",
  "estimated_impact": "medium"
}
```

### Производительность

```json
{
  "type": "performance",
  "file_path": "infrastructure/context_engine.py",
  "description": "Использование list comprehension вместо цикла for",
  "suggestion": "Заменить цикл for на list comprehension для улучшения производительности",
  "confidence": 1.0,
  "priority": 5,
  "reasoning": "List comprehension быстрее на 10-20% для простых операций",
  "estimated_impact": "low"
}
```

### Документация

```json
{
  "type": "documentation",
  "file_path": "agents/planner.py",
  "description": "Функция create_plan не имеет docstring",
  "suggestion": "Добавить docstring с описанием параметров и возвращаемого значения",
  "confidence": 1.0,
  "priority": 6,
  "reasoning": "Docstring улучшает читаемость и поддерживаемость кода",
  "estimated_impact": "medium"
}
```
