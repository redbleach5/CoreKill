# 📚 API Reference - Autonomous Improver v2-universal

**Версия:** v2-universal (Этап 2 завершён)  
**Дата:** 2026-01-23  
**Статус:** Универсальная реализация с поддержкой Python + Frontend

---

## 🎯 Публичный API

### Класс `AutonomousImprover`

#### Методы

##### `start() -> None`
Запускает фоновый анализ проекта.

**Описание:**
- Создаёт асинхронную задачу для периодического анализа
- Работает в фоне, не блокирует основной процесс
- Анализирует файлы с интервалом `cycle_interval_seconds`

**Использование:**
```python
from infrastructure.autonomous_improver import get_autonomous_improver

improver = get_autonomous_improver()
improver.start()
```

**Примечания:**
- Требует активный event loop
- Можно вызвать только один раз (singleton)
- Автоматически останавливается при shutdown приложения

---

##### `stop() -> None`
Останавливает фоновый анализ.

**Описание:**
- Отменяет асинхронную задачу
- Сохраняет накопленные предложения
- Очищает ресурсы

**Использование:**
```python
improver.stop()
```

**Примечания:**
- Безопасно вызывать несколько раз
- Предложения остаются доступными через `get_suggestions()`

---

##### `get_suggestions(min_confidence: Optional[float] = None) -> List[ImprovementSuggestion]`
Возвращает накопленные предложения по улучшению.

**Параметры:**
- `min_confidence` (Optional[float]): Минимальная уверенность (0.0-1.0). Если None, используется `self.min_confidence`.

**Возвращает:**
- `List[ImprovementSuggestion]`: Список предложений, отсортированных по приоритету и уверенности.

**Использование:**
```python
# Все предложения с минимальной уверенностью
suggestions = improver.get_suggestions()

# Только высокоуверенные (>= 0.95)
high_confidence = improver.get_suggestions(min_confidence=0.95)
```

**Структура `ImprovementSuggestion`:**
```python
@dataclass
class ImprovementSuggestion:
    type: ImprovementType  # Тип улучшения
    file_path: str  # Путь к файлу
    description: str  # Описание проблемы
    suggestion: str  # Конкретное предложение
    confidence: float  # Уверенность (0.0-1.0)
    priority: int  # Приоритет (1-10)
    reasoning: str  # Обоснование
    estimated_impact: str  # Оценка влияния
    code_example: Optional[str]  # Пример кода
    metadata: Dict[str, Any]  # Дополнительные данные
```

---

##### `get_metrics() -> Dict[str, Any]`
Возвращает метрики работы модуля.

**Возвращает:**
- `Dict[str, Any]`: Словарь с метриками:
  - `total_files_analyzed`: Общее количество проанализированных файлов
  - `total_suggestions`: Общее количество предложений
  - `high_confidence_suggestions`: Предложения с confidence >= min_confidence
  - `average_confidence`: Средняя уверенность
  - `suggestions_by_type`: Распределение по типам
  - `suggestions_by_priority`: Распределение по приоритетам
  - `cycles_without_progress`: Циклы без прогресса
  - `files_without_high_confidence`: Файлы без высокоуверенных предложений
  - `current_model`: Текущая используемая модель
  - `available_models_count`: Количество доступных моделей
  - `files_with_status`: Количество файлов со статусом

**Использование:**
```python
metrics = improver.get_metrics()
print(f"Найдено предложений: {metrics['total_suggestions']}")
print(f"Средняя уверенность: {metrics['average_confidence']:.2f}")
```

---

### Фабричные функции

##### `get_autonomous_improver() -> AutonomousImprover`
Возвращает глобальный экземпляр (singleton).

**Использование:**
```python
from infrastructure.autonomous_improver import get_autonomous_improver

improver = get_autonomous_improver()
```

**Примечания:**
- Создаёт экземпляр при первом вызове
- Использует настройки из `config.toml`
- Возвращает тот же экземпляр при последующих вызовах

---

##### `reset_autonomous_improver() -> None`
Сбрасывает глобальный экземпляр.

**Использование:**
```python
from infrastructure.autonomous_improver import reset_autonomous_improver

reset_autonomous_improver()
```

**Примечания:**
- Останавливает текущий экземпляр перед сбросом
- Полезно для тестирования или переинициализации

---

## 📋 Типы данных

### `ImprovementType` (Enum)
Типы улучшений:

**Общие типы:**
- `CODE_QUALITY` - Улучшение качества кода (читаемость, поддерживаемость)
- `PERFORMANCE` - Оптимизация производительности
- `SECURITY` - Улучшение безопасности
- `DOCUMENTATION` - Добавление документации
- `REFACTORING` - Рефакторинг
- `ARCHITECTURE` - Архитектурные улучшения

**Фронтенд-специфичные типы:**
- `ACCESSIBILITY` - Доступность (a11y) для UI/UX кода
- `UX` - User experience улучшения
- `TYPES` - TypeScript типизация и типы
- `COMPONENT_DESIGN` - Структура компонентов

---

## ⚠️ Обратная совместимость

API v2-universal полностью обратно совместим с v1-python-specific:

- ✅ Все методы v1 остаются доступными
- ✅ Сигнатуры методов не изменились
- ✅ Поведение идентично для Python-проектов
- ✅ Новые возможности опциональны (адаптеры, профили)
- ✅ Python-проекты работают как раньше (автоматический выбор `PythonAdapter`)

---

## 🔗 Связанные документы

- [README.md](README.md) - Общая документация
- [REFACTORING_PLAN.md](REFACTORING_PLAN.md) - План универсализации
- [REFACTORING_PLAN_UPDATED.md](REFACTORING_PLAN_UPDATED.md) - Обновлённый план с фронтенд-поддержкой
