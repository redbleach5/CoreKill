Отлично. Ниже — **чёткий, инженерно-прагматичный пошаговый план**, без философии и без «переписать всё». План рассчитан так, чтобы **на каждом шаге система оставалась рабочей**.

---

# 🧭 Пошаговый план универсализации Autonomous Improver

## 🎯 Цель

Превратить текущий модуль в **универсальное ядро анализа проектов**, расширяемое под:

* любой язык
* любой стек
* любой тип проекта
  через **адаптеры и профили**, а не хардкод.

---

## Фаза 0 — Зафиксировать базу (1 шаг)

### Шаг 0.1 — Зафиксировать текущую реализацию

**Что сделать**

* Пометить текущую версию как `v1-python-specific`
* Зафиксировать публичный API:

  * `start()`
  * `stop()`
  * `get_suggestions()`
  * `get_metrics()`

**Зачем**

* Это будет эталон поведения
* Любая универсализация не должна ломать UX

---

## Фаза 1 — Отделить ядро от Python (критично)

### Шаг 1.1 — Переименовать смысл, не код

**Что сделать**

* `AutonomousImprover` → `AutonomousAnalyzerCore`
* Файл оставить тот же (пока)

**Зачем**

* Ментально отделяем «что делает» от «как»

---

### Шаг 1.2 — Ввести интерфейс LanguageAdapter

**Что сделать**
Создать `adapters/base.py`:

```python
class LanguageAdapter(ABC):
    language: str

    def discover_files(self, project_path) -> list[Path]: ...
    def analyze_structure(self, file_path) -> Any: ...
    def build_context(self, file_path, structure) -> str: ...
    def extract_code_sample(self, file_path, structure, max_chars) -> str: ...
    def build_search_query(self, file_path, structure) -> str: ...
    def validate_suggestion(self, suggestion) -> bool: ...
```

**Зачем**

* Это точка расширения под любой язык
* Всё, что сейчас Python-specific, будет мигрировать сюда

---

### Шаг 1.3 — Создать PythonAdapter (без изменений логики)

**Что сделать**

* Вынести:

  * `*.py` фильтрацию
  * `ASTAnalyzer`
  * `_extract_code_sample`
  * `_build_search_query`
  * PEP8 / docstring эвристики
* В `adapters/python_adapter.py`

**Важно**
❌ не улучшать
❌ не оптимизировать
✅ только перенести

---

### Шаг 1.4 — Подключить адаптер в ядро

**Что сделать**
В `AutonomousAnalyzerCore.__init__`:

```python
self.adapter = adapter or PythonAdapter()
```

Заменить:

* `project_path.rglob("*.py")`
* AST-логику
  на вызовы `self.adapter.*`

**Результат**
👉 Поведение **идентично**, но Python стал плагином.

---

## Фаза 2 — Вынести знания о проекте

### Шаг 2.1 — Ввести ProjectProfile

**Что сделать**
Создать `profiles/project_profile.py`:

```python
@dataclass
class ProjectProfile:
    language: str
    framework: Optional[str]
    domain: Optional[str]
    quality_rules: dict
    priorities: dict
    confidence_policy: dict
```

---

### Шаг 2.2 — Убрать доменные if-else из кода

**Что вынести**

```python
if "agent" in file_name:
if "fastapi" in file_name:
```

**Куда**
В `profile.framework`

**Результат**

* Ядро ничего не знает о FastAPI / agent / backend

---

### Шаг 2.3 — Привязать профиль к адаптеру

```python
adapter = PythonAdapter(profile)
```

**Зачем**

* Адаптер решает, *что считать плохим кодом*
* Ядро — только оркестрирует

---

## Фаза 3 — Универсальный prompt

### Шаг 3.1 — Вынести ANALYSIS_PROMPT в PromptBuilder

**Что сделать**
Создать:

```python
class PromptBuilder:
    def build(adapter, profile, context, code, web_context) -> str
```

**Результат**

* Prompt больше не строка
* Он зависит от языка, проекта, приоритетов

---

### Шаг 3.2 — Убрать Python-термины из prompt

Заменить:

* PEP8 → “project coding standards”
* docstring → “documentation comments”

---

## Фаза 4 — Универсальная модель улучшений

### Шаг 4.1 — Заменить ImprovementType

**Было**

```python
CODE_QUALITY, DOCUMENTATION, PEP8
```

**Стало**

```python
READABILITY
MAINTAINABILITY
PERFORMANCE
SECURITY
CORRECTNESS
ARCHITECTURE
DX
```

---

### Шаг 4.2 — Маппинг типов делегировать адаптеру

```python
adapter.map_llm_type(llm_type) -> ImprovementCategory
```

---

## Фаза 5 — Универсальная confidence-модель

### Шаг 5.1 — Ввести ConfidenceAccumulator

**Что сделать**

```python
class ConfidenceAccumulator:
    def update(file_hash, suggestion_hash, confidence)
    def effective_confidence(...) -> float
```

**Факторы**

* повторяемость
* стабильность кода
* AST-подтверждение
* web-источники

---

### Шаг 5.2 — Убрать «1.0 сразу»

* `min_confidence = 0.95`
* `1.0` достигается накоплением

---

## Фаза 6 — Web-search как плагин

### Шаг 6.1 — Ввести KnowledgeProvider

```python
class KnowledgeProvider:
    def enrich(query) -> str
```

Реализации:

* Tavily
* NoneProvider
* LocalDocs

---

## Фаза 7 — Минимальный Generic Adapter

### Шаг 7.1 — GenericTextAdapter

**Что делает**

* анализирует `.txt`, `.md`, `.cfg`
* только архитектура / документация / DX

**Зачем**

* модуль реально становится универсальным

---

## 🏁 Финальное состояние

Ты получаешь систему, которая:

* 🔌 подключается к любому проекту
* 🧠 знает *как* анализировать, но не *что считать истиной*
* 📦 расширяется без правки ядра
* 🤖 может стать IDE-агентом, CI-анализатором или продуктом

---
