# 🎯 Обновлённый план универсализации с учётом фронтенд-кода

**Дата:** 2026-01-23  
**Обновление:** Добавлена потребность в анализе UI/UX кода (JSON, JS, TS, TSX, HTML, MD)

---

## 🔄 Изменение приоритетов

### Было: "nice to have"
### Стало: **Реальная потребность**

**Почему:**
- ✅ Нужен анализ фронтенд-кода (JS, TS, TSX, HTML, MD, JSON)
- ✅ Улучшение UI/UX требует понимания структуры компонентов
- ✅ План становится практически значимым

---

## 📋 Адаптированный план

### Фаза 0 — Зафиксировать базу ⭐⭐⭐⭐⭐

**Статус:** Критично  
**Время:** 1 день

**Что сделать:**
- Зафиксировать текущий API
- Создать `v1-python-specific` baseline
- Документировать текущее поведение

**Зачем:**
- Точка отсчёта для всех изменений
- Гарантия обратной совместимости

---

### Фаза 1 — LanguageAdapter (обновлённый приоритет) ⭐⭐⭐⭐⭐

**Статус:** Критично (теперь!)  
**Время:** 3-5 дней

**Почему теперь критично:**
- Нужна поддержка JS/TS/TSX/HTML/MD/JSON
- Python-only больше не достаточен

**План реализации:**

#### Шаг 1.1 — Создать базовый интерфейс
```python
class LanguageAdapter(ABC):
    language: str
    file_extensions: list[str]
    
    def discover_files(self, project_path) -> list[Path]: ...
    def analyze_structure(self, file_path) -> Any: ...
    def build_context(self, file_path, structure) -> str: ...
    def extract_code_sample(self, file_path, structure, max_chars) -> str: ...
    def build_search_query(self, file_path, structure) -> str: ...
    def validate_suggestion(self, suggestion) -> bool: ...
```

#### Шаг 1.2 — PythonAdapter (рефакторинг текущего)
- Вынести всю Python-специфичную логику
- Сохранить текущее поведение

#### Шаг 1.3 — FrontendAdapter (новый!)
**Поддержка:**
- `.js`, `.ts`, `.tsx` — TypeScript/JavaScript
- `.html` — HTML разметка
- `.md` — Markdown документация
- `.json` — JSON конфигурация

**Что анализировать:**
- Структура компонентов (React/Vue/etc)
- TypeScript типы и интерфейсы
- Accessibility (a11y) проблемы
- Performance антипаттерны
- UX проблемы (недоступные элементы, плохая навигация)
- Документация (README, комментарии)

**Метрики:**
- Размер компонентов
- Глубина вложенности
- Использование хуков
- Пропсы/типы
- Accessibility атрибуты

---

### Фаза 2 — ProjectProfile ⭐⭐⭐⭐⭐

**Статус:** Критично  
**Время:** 2-3 дня

**Обновление:**
Добавить профили для фронтенд-проектов:

```python
@dataclass
class ProjectProfile:
    language: str  # "python" | "typescript" | "javascript" | "mixed"
    framework: Optional[str]  # "react" | "vue" | "fastapi" | None
    domain: Optional[str]  # "frontend" | "backend" | "fullstack"
    quality_rules: dict
    priorities: dict
    confidence_policy: dict
```

**Профили:**
- `python-backend` — текущий
- `typescript-frontend` — React/TypeScript
- `mixed-fullstack` — Python + TypeScript

---

### Фаза 3 — Универсальный prompt ⭐⭐⭐⭐

**Статус:** Важно  
**Время:** 2 дня

**Обновление:**
Prompt должен учитывать:
- Язык (Python vs TypeScript)
- Тип файла (компонент vs утилита)
- Контекст (UI/UX vs бизнес-логика)

**Примеры:**
- Для `.tsx` файлов: React best practices, accessibility
- Для `.md` файлов: документация, структура
- Для `.json` файлов: валидность, структура

---

### Фаза 4 — Универсальная модель улучшений ⭐⭐⭐⭐

**Статус:** Важно  
**Время:** 2 дня

**Обновление:**
Добавить категории для фронтенда:

```python
class ImprovementType(str, Enum):
    # Общие
    READABILITY = "readability"
    MAINTAINABILITY = "maintainability"
    PERFORMANCE = "performance"
    SECURITY = "security"
    ARCHITECTURE = "architecture"
    
    # Фронтенд-специфичные
    ACCESSIBILITY = "accessibility"  # a11y
    UX = "ux"  # User experience
    DX = "dx"  # Developer experience
    TYPES = "types"  # TypeScript типы
    COMPONENT_DESIGN = "component_design"  # Структура компонентов
```

---

### Фаза 5 — ConfidenceAccumulator ⭐⭐⭐⭐⭐

**Статус:** Критично  
**Время:** 2-3 дня

**Без изменений** — уже частично реализовано, нужно доработать.

---

### Фаза 6 — Web-search как плагин ⭐⭐⭐

**Статус:** Средний приоритет  
**Время:** 1-2 дня

**Можно отложить** — текущая реализация работает.

---

### Фаза 7 — Generic Adapter ⭐⭐⭐

**Статус:** Средний приоритет  
**Время:** 1-2 дня

**Обновление:**
Вместо GenericTextAdapter → **MarkdownAdapter**
- Анализ документации
- Структура README
- Ссылки и примеры

---

## 🚀 Рекомендуемый порядок выполнения

### Этап 1: База (1 неделя)
1. ✅ Фаза 0 — Зафиксировать базу
2. ✅ Фаза 5 — ConfidenceAccumulator (доработать)
3. ✅ Фаза 2 — ProjectProfile (базовый)

### Этап 2: Фронтенд-поддержка (1-2 недели)
4. ✅ Фаза 1 — LanguageAdapter + FrontendAdapter
5. ✅ Фаза 3 — Универсальный prompt (с фронтенд-контекстом)
6. ✅ Фаза 4 — Универсальная модель улучшений

### Этап 3: Полировка (опционально)
7. ⚠️ Фаза 6 — Web-search как плагин
8. ⚠️ Фаза 7 — MarkdownAdapter

---

## 💡 Конкретные улучшения для фронтенда

### TypeScript/JavaScript
- Отсутствующие типы
- `any` использование
- Неиспользуемые импорты
- Большие компоненты (>300 строк)
- Отсутствие PropTypes/типов для пропсов

### React/TSX
- Accessibility проблемы (нет `alt`, `aria-*`)
- Performance (отсутствие `memo`, `useMemo`)
- Хуки антипаттерны (зависимости, порядок)
- Неоптимальные ререндеры

### HTML
- Семантическая разметка
- Accessibility атрибуты
- SEO мета-теги
- Валидность HTML

### Markdown
- Структура документации
- Отсутствующие примеры
- Устаревшие ссылки
- Плохое форматирование

### JSON
- Валидность схемы
- Дублирование
- Неиспользуемые ключи
- Неоптимальная структура

---

## 🎯 Итоговая оценка

### Ценность плана: **9/10** (было 8/10)

**Почему выше:**
- ✅ Есть реальная потребность (фронтенд-код)
- ✅ План решает практическую задачу
- ✅ LanguageAdapter становится критичным

**Риски:**
- ⚠️ Сложность реализации FrontendAdapter
- ⚠️ Нужны инструменты для анализа TS/JS (ESLint, TypeScript parser)

**Рекомендация:**
- Начать с Этапа 1 (база)
- Затем Этап 2 (фронтенд-поддержка)
- Этап 3 — по необходимости

---

## 📝 Следующие шаги

1. **Зафиксировать текущее состояние** (Фаза 0)
2. **Создать FrontendAdapter** для JS/TS/TSX/HTML/MD
3. **Интегрировать в ядро** через LanguageAdapter
4. **Протестировать на фронтенд-коде**

**Время:** 2-3 недели для полной реализации  
**Результат:** Универсальный анализатор для Python + Frontend
