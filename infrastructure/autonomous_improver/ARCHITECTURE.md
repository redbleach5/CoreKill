# 🏗️ Архитектура Autonomous Improver

**Версия:** v2-universal  
**Дата:** 2026-01-23

---

## 📐 Обзор архитектуры

Модуль построен на принципах:
- **Разделение ответственности** - ядро отделено от языково-специфичной логики
- **Расширяемость** - поддержка новых языков через адаптеры
- **Конфигурируемость** - настройка под разные типы проектов через профили
- **Обратная совместимость** - все изменения не ломают существующий API

---

## 🧩 Компоненты

### 1. Ядро (`core.py`)

**`AutonomousImprover`** - основной класс модуля.

**Ответственность:**
- Управление фоновым анализом
- Координация между адаптерами и профилями
- Накопление предложений
- Управление метриками

**Зависимости:**
- `LanguageAdapter` - для языково-специфичного анализа
- `ProjectProfile` - для конфигурации проекта
- `ConfidenceAccumulator` - для накопления уверенности
- `PromptBuilder` - для построения промптов

---

### 2. Адаптеры (`adapters/`)

**Интерфейс:** `LanguageAdapter` (абстрактный класс)

**Реализации:**
- **`PythonAdapter`** - анализ Python кода (`.py`)
  - AST-анализ через `ASTAnalyzer`
  - PEP 8 соответствие
  - Docstrings и документация

- **`FrontendAdapter`** - анализ фронтенд-кода
  - TypeScript/JavaScript (`.ts`, `.tsx`, `.js`) - компоненты, хуки, типы
  - HTML (`.html`) - семантика, accessibility
  - Markdown (`.md`) - структура документации
  - JSON (`.json`) - валидность, структура

- **`MultiAdapter`** - для mixed проектов
  - Объединяет несколько адаптеров
  - Автоматически выбирает подходящий адаптер для каждого файла

**Методы адаптера:**
```python
- discover_files(project_path) -> List[Path]
- analyze_structure(file_path) -> Any
- build_context(file_path, structure) -> str
- extract_code_sample(file_path, structure, max_chars) -> str
- build_search_query(file_path, structure) -> str
- validate_suggestion(suggestion) -> bool
```

---

### 3. Профили (`project_profile.py`)

**`ProjectProfile`** - конфигурация проекта.

**Содержит:**
- Язык проекта (`python`, `typescript`, `javascript`, `mixed`)
- Домен (`backend`, `frontend`, `fullstack`, `library`, `script`)
- Фреймворк (`fastapi`, `react`, `vue`, `django`, `flask`)
- Правила качества (`QualityRules`)
- Приоритеты (`Priorities`)
- Политика уверенности (`ConfidencePolicy`)
- Исключённые файлы/директории

**Автоопределение:**
```python
profile = ProjectProfile.detect_from_project(project_path)
# Определяет язык, фреймворк, домен на основе файлов проекта
```

---

### 4. Накопление уверенности (`confidence_accumulator.py`)

**`ConfidenceAccumulator`** - решает проблему "LLM никогда не 100% уверен".

**Механизм:**
1. **Базовая уверенность** - от LLM (0.0-1.0)
2. **Бонусы:**
   - Повторение предложения (стабильность)
   - Стабильность кода (файл не менялся)
   - AST/структурное подтверждение
   - Веб-подтверждение (best practices)
3. **Эффективная уверенность** = базовая + бонусы (макс. 1.0)

**История:**
- Хранит наблюдения за предложениями
- Отслеживает изменения файлов
- Очищает старую историю (управление памятью)

---

### 5. Построитель промптов (`prompt_builder.py`)

**`PromptBuilder`** - создаёт динамические промпты для LLM.

**Особенности:**
- Базовый промпт + языково-специфичные правила
- Доменно-специфичные правила (backend, frontend)
- Динамический выбор языка подсветки кода
- Интеграция контекста, кода, веб-результатов

**Примеры:**
- Python → PEP 8, docstrings, type hints
- TypeScript → ESLint, accessibility, React best practices
- HTML → семантика, a11y, SEO

---

## 🔄 Поток работы

```
1. Инициализация
   ├─ Определение ProjectProfile (авто или явно)
   ├─ Выбор LanguageAdapter (PythonAdapter / FrontendAdapter / MultiAdapter)
   └─ Настройка ConfidenceAccumulator

2. Цикл анализа
   ├─ Discovery: adapter.discover_files()
   ├─ Фильтрация: profile.should_analyze_file()
   ├─ Приоритизация: _prioritize_files()
   ├─ Кэширование: проверка хеша файла
   └─ Параллельный анализ (3-5 файлов)

3. Анализ файла
   ├─ Структурный анализ: adapter.analyze_structure()
   ├─ Построение контекста: adapter.build_context()
   ├─ Выборка кода: adapter.extract_code_sample()
   ├─ Построение промпта: PromptBuilder.build()
   ├─ LLM анализ
   ├─ Веб-поиск (если нужно)
   ├─ Парсинг предложений
   ├─ Валидация: adapter.validate_suggestion()
   └─ Обновление ConfidenceAccumulator

4. Накопление предложений
   ├─ Фильтрация по эффективной уверенности
   ├─ Удаление дубликатов
   └─ Rotation (макс. 200 предложений)
```

---

## 📦 Структура модуля

```
infrastructure/autonomous_improver/
├── __init__.py                    # Публичный API
├── core.py                        # Ядро (AutonomousImprover)
├── project_profile.py              # Профили проектов
├── confidence_accumulator.py      # Накопление уверенности
├── prompt_builder.py               # Построитель промптов
├── adapters/
│   ├── __init__.py
│   ├── base.py                    # LanguageAdapter (интерфейс)
│   ├── python_adapter.py          # Python анализ
│   ├── frontend_adapter.py        # Frontend анализ
│   └── multi_adapter.py           # Mixed проекты
├── scripts/
│   ├── test.py                    # Тестовый скрипт
│   ├── run.sh                     # Скрипт запуска
│   └── analyze_results.py         # Анализ результатов
└── [документация]
```

---

## 🔌 Расширяемость

### Добавление нового языка

1. Создать новый адаптер:
```python
class NewLanguageAdapter(LanguageAdapter):
    @property
    def language(self) -> str:
        return "new_language"
    
    @property
    def file_extensions(self) -> List[str]:
        return [".ext1", ".ext2"]
    
    # Реализовать все методы интерфейса
```

2. Зарегистрировать в `MultiAdapter` или использовать напрямую:
```python
improver = AutonomousImprover(adapter=NewLanguageAdapter())
```

### Добавление нового типа улучшения

1. Добавить в `ImprovementType` enum:
```python
class ImprovementType(str, Enum):
    ...
    NEW_TYPE = "new_type"
```

2. Обновить `PromptBuilder` для поддержки нового типа
3. Обновить fuzzy matching в `core.py` (опционально)

---

## 🎯 Принципы проектирования

1. **Single Responsibility** - каждый компонент имеет одну ответственность
2. **Open/Closed** - открыт для расширения, закрыт для модификации
3. **Dependency Inversion** - ядро зависит от абстракций (адаптеры), а не от конкретных реализаций
4. **Interface Segregation** - `LanguageAdapter` содержит только необходимые методы
5. **DRY** - общая логика вынесена в ядро, языково-специфичная - в адаптеры

---

## 🔗 Связанные документы

- [README.md](README.md) - Общая документация
- [API_REFERENCE.md](API_REFERENCE.md) - Детальная документация API
- [VERSION.md](VERSION.md) - Версионирование
- [CHANGELOG.md](CHANGELOG.md) - История изменений
