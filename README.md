# Cursor Killer - Локальная многоагентная система генерации кода

Полностью локальная система для генерации и редактирования кода, превосходящая качество Cursor AI в большинстве сценариев.

## 🚀 Быстрый старт

### Требования

- Python 3.12+
- Node.js 18+ (для frontend)
- Ollama с установленными моделями (см. `config.toml` для настройки):
  - Основная модель для кода (по умолчанию из config.toml)
  - Модель для классификации намерений (легкая модель)
  - `nomic-embed-text` (для embeddings RAG)

### Установка

1. **Установите Python зависимости:**
```bash
pip install -r requirements.txt
```

2. **Установите и запустите Ollama:**
```bash
# Установите модели
ollama pull codellama:13b-instruct-q4_0
ollama pull tinyllama:1.1b
ollama pull nomic-embed-text
```

3. **Установите Frontend зависимости:**
```bash
cd frontend
npm install
```

## 🏃 Запуск

### Вариант 1: Автоматический запуск (рекомендуется)

**Единая команда для запуска всего проекта:**
```bash
python run.py
```

Скрипт автоматически:
- ✅ Проверяет окружение (Python, Node.js, зависимости)
- ✅ Запускает backend и frontend
- ✅ Проверяет здоровье сервисов
- ✅ Мониторит ошибки в реальном времени
- ✅ Корректно завершает процессы при остановке (Ctrl+C)

Откройте браузер: `http://localhost:5173`

> ⚠️ **Важно:** Задачи выполняются в памяти и **не сохраняются** при перезапуске backend или frontend. 
> Если вы перезапустите сервисы во время выполнения задачи, выполнение будет прервано и результаты не сохранятся. 
> Дождитесь завершения задачи перед перезапуском.

### Вариант 2: Ручной запуск

**Terminal 1 - Backend:**
```bash
uvicorn backend.api:app --reload --port 8000
```

**Terminal 2 - Frontend:**
```bash
cd frontend
npm run dev
```

Откройте браузер: `http://localhost:5173`

### Вариант 3: CLI интерфейс

```bash
python cli.py
```

## 📋 Основные возможности

- ✅ **Многоагентная архитектура**: Intent → Planner → Researcher → TestGenerator → Coder → Validator → Debugger → Reflection
- ✅ **LangGraph workflow**: Workflow реализован через LangGraph для управления состоянием и переходами
- ✅ **Локальная работа**: 100% на Ollama + ChromaDB (веб-поиск опциональный)
- ✅ **TDD подход**: Тесты генерируются ДО кода
- ✅ **Self-Healing**: Автоматическое исправление ошибок через Debugger Agent (до 3 итераций)
- ✅ **Рефлексия и обучение**: Система анализирует результаты и сохраняет опыт
- ⚠️ **Временное хранение**: Задачи выполняются в памяти и не сохраняются при перезапуске (результаты сохраняются в `output/` после завершения)
- ✅ **Память**: ChromaDB для поиска похожих задач и рекомендаций
- ✅ **Валидация**: pytest, mypy, bandit с автоматическим исправлением ошибок
- ✅ **Автосохранение**: Все артефакты в `./output/{timestamp}/`
- ✅ **Режимы**: create, modify, debug, optimize, explain, test, refactor
- ✅ **Real-time UI**: SSE стриминг прогресса с анимациями

## 📁 Структура проекта

```
cursor-killer/
├── agents/              # Агенты системы
│   ├── intent.py       # Определение намерения
│   ├── planner.py      # Планирование
│   ├── researcher.py   # Сбор контекста (RAG + веб)
│   ├── test_generator.py  # Генерация тестов
│   ├── coder.py        # Генерация кода
│   ├── debugger.py     # Анализ ошибок и self-healing
│   ├── reflection.py   # Рефлексия и оценка
│   └── memory.py       # Система памяти
├── backend/            # FastAPI backend
│   ├── api.py         # Главное приложение
│   └── routers/       # API роутеры
├── frontend/          # React + TypeScript + Vite
│   └── src/
│       ├── components/  # React компоненты
│       └── hooks/      # React хуки
├── infrastructure/    # LLM, RAG, веб-поиск, workflow
│   ├── workflow_state.py  # State схема для LangGraph
│   ├── workflow_nodes.py  # Узлы графа
│   ├── workflow_edges.py  # Условные переходы
│   └── workflow_graph.py  # Граф LangGraph
├── utils/            # Утилиты
├── config.toml       # Конфигурация
└── output/           # Сохранённые артефакты
```

## ⚙️ Конфигурация

Редактируйте `config.toml`:

```toml
[default]
default_model = "codellama:13b-instruct-q4_0"
max_iterations = 5
enable_web = true
temperature = 0.25
max_tokens_warning = 30000
output_dir = "output"
```

## 📝 Примеры использования

### CLI

```bash
python cli.py
> Введите задачу: Создать класс для работы с конфигурацией из TOML файла
```

### Веб-интерфейс

1. Откройте `http://localhost:5173`
2. Введите задачу в текстовое поле
3. Настройте параметры в sidebar (модель, температура, веб-поиск)
4. Нажмите "Запустить"
5. Смотрите прогресс в реальном времени

### Режим modify/debug

```bash
# В задаче укажите путь к файлу
"Исправить ошибку в файле: src/utils/validator.py"
```

Система автоматически загрузит файл как контекст.

## 🎨 UI Компоненты

Система использует shadcn/ui. Добавьте компоненты:

```bash
cd frontend
npx shadcn@latest add button card progress accordion badge alert
```

## 📊 Качественные метрики

- Покрытие тестами: ≥ 85-92%
- mypy strict: 0 ошибок
- bandit: уровень medium и ниже
- Confidence агентов: ≥ 0.75

## 🔧 Разработка

### Запуск тестов

```bash
pytest tests/
```

### Linting

```bash
mypy . --strict
bandit -r . -ll
```

## 📚 Документация

API документация доступна по адресу: `http://localhost:8000/docs`

## 🐛 Troubleshooting

**Проблема**: Ollama не отвечает
- Проверьте что Ollama запущен: `ollama list`
- Убедитесь что модели установлены

**Проблема**: CORS ошибки
- Проверьте что backend на порту 8000, frontend на 5173
- Проверьте настройки CORS в `backend/api.py`

**Проблема**: RAG не работает
- Проверьте что `nomic-embed-text` установлен: `ollama pull nomic-embed-text`
- Проверьте директорию `.chromadb` (создаётся автоматически)

## 📄 Лицензия

MIT

## 🤝 Вклад

Приветствуются PR и issues!
