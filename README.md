# Cursor Killer — Локальная многоагентная система генерации кода

Полностью локальная система для генерации и редактирования кода с гибридным UX, встроенной IDE, умным выбором моделей и поддержкой reasoning моделей (DeepSeek-R1).

## 🚀 Быстрый старт

### Требования

- Python 3.12+
- Node.js 18+
- Ollama

### Установка и запуск

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/redbleach5/CoreKill.git
cd CoreKill

# 2. Установите Ollama модели
ollama pull qwen2.5-coder:7b    # Для генерации кода
ollama pull deepseek-r1:8b      # Reasoning модель (рекомендуется)
ollama pull phi3:mini           # Для chat (или любая лёгкая модель)
ollama pull nomic-embed-text    # Для RAG

# 3. Запустите (автоустановка зависимостей)
python3 run.py
```

Скрипт `run.py` автоматически:
- Создаст виртуальное окружение `.venv`
- Установит Python зависимости
- Установит Node.js зависимости
- Запустит backend и frontend

Откройте браузер: `http://localhost:5173`

## ✨ Основные возможности

### Гибридный UX
- **Авто** — умный выбор режима по запросу
- **Диалог** — быстрые ответы на вопросы (лёгкая модель)
- **Код** — полный workflow генерации кода (мощная модель)

### Встроенная IDE
- Редактор кода с подсветкой синтаксиса Python
- Выполнение кода прямо в браузере
- Многофайловый режим (табы)
- Меню проекта с индексацией кодовой базы

### История чатов
- Сайдбар со списком всех диалогов
- Поиск по диалогам
- Переключение между диалогами без потери контекста
- Автосохранение на сервере

### Умный выбор моделей
Система автоматически выбирает модель по сложности задачи:

| Сложность | Примеры | Модель |
|-----------|---------|--------|
| SIMPLE | "привет", "что умеешь?" | phi3:mini (быстрая) |
| MEDIUM | "объясни async/await" | qwen2.5-coder:7b |
| COMPLEX | "напиши игру змейка" | qwen2.5-coder:7b или 13B+ |

### Многоагентная архитектура
```
Intent → Planner → Researcher → TestGenerator → Coder → Validator → Debugger → Reflection
```

### Ключевые фичи
- ✅ **100% локальная работа** — Ollama + ChromaDB
- ✅ **LangGraph workflow** — управление состоянием и переходами
- ✅ **Reasoning Models** — поддержка DeepSeek-R1, QwQ с chain-of-thought
- ✅ **Structured Output** — Pydantic валидация ответов LLM
- ✅ **TDD подход** — тесты генерируются ДО кода
- ✅ **Self-Healing** — автоисправление ошибок (до 3 итераций)
- ✅ **История диалогов** — с автоматической суммаризацией
- ✅ **Встроенная IDE** — редактор + выполнение кода
- ✅ **Индексация проекта** — контекст из вашего кода
- ✅ **Веб-поиск** — автоматический поиск для актуальных запросов
- ✅ **Удалённый Ollama** — подключение к GPU-серверу через Tailscale

## 📁 Структура проекта

```
cursor-killer/
├── agents/              # Агенты системы
│   ├── intent.py        # Определение намерения и сложности
│   ├── chat.py          # Агент для диалогов
│   ├── conversation.py  # Управление историей диалогов
│   ├── planner.py       # Планирование
│   ├── coder.py         # Генерация кода
│   └── ...
├── backend/             # FastAPI backend
│   └── routers/agent.py # API endpoints
├── frontend/            # React + TypeScript + Vite
│   └── src/
│       ├── App.tsx
│       ├── components/
│       │   ├── chat/           # Компоненты чата
│       │   ├── header/         # Шапка приложения
│       │   ├── ChatHistory.tsx # Сайдбар истории
│       │   ├── IDEPanel.tsx    # Встроенная IDE
│       │   └── CodeEditor.tsx  # Редактор кода
│       └── hooks/
│           └── useAgentStream.ts  # SSE hook
├── infrastructure/      # LLM, RAG, model router, workflow
│   ├── model_router.py  # Умный выбор моделей
│   ├── workflow_*.py    # LangGraph workflow
│   └── ...
├── config.toml          # Конфигурация
└── output/              # Сохранённые артефакты
    └── conversations/   # История диалогов (JSON)
```

## ⚙️ Конфигурация

`config.toml`:

```toml
# Подключение к удалённому Ollama (GPU-сервер)
[ollama]
host = "http://localhost:11434"  # или Tailscale IP: "http://100.x.x.x:11434"

[default]
default_model = "qwen2.5-coder:7b"
temperature = 0.25

[interaction]
default_mode = "auto"           # auto, chat, code
chat_model = "phi3:mini"        # Лёгкая модель для chat

# Reasoning модели (DeepSeek-R1, QwQ)
[reasoning]
prefer_reasoning_models = true  # Использовать для complex задач
show_thinking = false           # Показывать <think> блоки

# Structured Output через Pydantic
[structured_output]
enabled = true
max_retries = 2

[hardware]
max_model_vram_gb = 0           # 0 = без лимита
allow_heavy_models = true       # Разрешить 30B+ модели

[persistence]
enabled = true
checkpoint_directory = ".task_checkpoints"
```

### Удалённый Ollama (GPU-сервер)

Для использования мощного GPU (RTX 3090 и т.д.) с разработки на MacBook:

1. Установите [Tailscale](https://tailscale.com/) на обе машины (работает в РФ)
2. На машине с GPU:
   ```bash
   OLLAMA_HOST=0.0.0.0:11434 ollama serve
   ```
3. В `config.toml` укажите Tailscale IP:
   ```toml
   [ollama]
   host = "http://100.x.x.x:11434"
   ```

## 📊 API

### SSE Streaming
```
GET /api/stream?task=...&mode=auto
```

### Диалоги
```
GET /api/conversations           # Список диалогов
GET /api/conversations/{id}      # Получить диалог
DELETE /api/conversations/{id}   # Удалить диалог
```

### Индексация проекта
```
POST /api/index
Body: { "project_path": "/path/to/project", "file_extensions": [".py", ".js"] }
```

Полная документация: `http://localhost:8000/docs`

## 🔧 Разработка

```bash
# Тесты
pytest tests/

# Linting
mypy . --strict
ruff check .
```

## 📚 Документация

- [ARCHITECTURE.md](ARCHITECTURE.md) — архитектура системы
- [DEPLOYMENT.md](DEPLOYMENT.md) — развёртывание
- [future/ROADMAP_2026.md](future/ROADMAP_2026.md) — roadmap развития
- [future/russia.md](future/russia.md) — работа в РФ, доступность сервисов

## 🐛 Troubleshooting

**Ollama не отвечает:**
```bash
ollama list  # Проверить что запущен
```

**Медленные ответы на простые вопросы:**
- Проверьте что установлена лёгкая модель: `ollama pull phi3:mini`
- На MacBook M1/M2 модели 7B работают медленнее — используйте удалённый GPU

**Таймаут при генерации кода:**
- Увеличьте таймаут в `config.toml`: `[timeouts] coding = 300`
- Или подключите удалённый Ollama с мощным GPU

**CORS ошибки:**
- Backend должен быть на порту 8000, frontend на 5173

## 🌐 Работа в РФ

Проект полностью работает в России:
- **Tailscale** — для связи с GPU-сервером (работает без проблем)
- **Ollama** — локально, не требует внешних API
- См. [future/russia.md](future/russia.md) для полного списка

## 📄 Лицензия

MIT
