# Полное понимание проекта "Cursor Killer Pro"

## 📋 Общая концепция проекта

**"Cursor Killer Pro"** — это локальная многоагентная система для автоматической разработки кода, предназначенная стать полноценной заменой коммерческого Cursor AI. Система работает полностью оффлайн, используя локальные LLM-модели через Ollama и строго следует принципам Test-Driven Development (TDD).

### Ключевые принципы

1. **100% локальная работа** — все модели, RAG-база и инструменты работают локально без подключения к облачным сервисам
2. **Русский интерфейс** — весь CLI, логи и сообщения пользователю на русском языке
3. **Английский код** — весь код, комментарии, имена файлов и документация на английском
4. **Строгий TDD** — тесты пишутся ДО кода, покрытие ≥85%
5. **Отсутствие "запаха кода"** — соблюдение принципов SOLID, DRY, KISS
6. **Self-Healing** — автоматическая диагностика и исправление ошибок через Debugger Agent
7. **Long-Term Memory** — сохранение и семантический поиск прошлых решений в ChromaDB

---

## 🏗️ Архитектура системы

### Основа: LangGraph

Система построена на направленном графе состояний (LangGraph), где каждый агент — узел, а переходы зависят от результатов работы и валидации. Workflow реализован через LangGraph для управления состоянием и условными переходами между агентами.

### Структура данных State

State определён в `infrastructure/workflow_state.py`:

```python
class AgentState(TypedDict):
    task: str                    # Исходная задача пользователя
    max_iterations: int          # Максимальное количество итераций self-healing
    disable_web_search: bool     # Отключить веб-поиск
    model: Optional[str]         # Модель Ollama
    temperature: float          # Температура генерации
    intent_result: Optional[IntentResult]  # Результат Intent агента
    plan: str                   # План от Planner
    context: str                # Контекст от Researcher
    tests: str                  # Тесты от TestGenerator
    code: str                   # Код от Coder
    validation_results: Dict[str, Any]  # Результаты валидации
    debug_result: Optional[DebugResult]  # Результат Debugger
    reflection_result: Optional[ReflectionResult]  # Результат Reflection
    iteration: int              # Счетчик итераций self-healing
    task_id: str               # ID задачи
    enable_sse: bool        # Флаг для SSE стриминга
    file_path: Optional[str]  # Путь к файлу для modify/debug
    file_context: Optional[str]  # Контекст файла
```

---

## 🤖 Агенты и их роли

| Агент | Роль | Модель (RTX 3090) | Ключевая функция |
| :--- | :--- | :--- | :--- |
| **Intent Router** | Классификация запроса и выбор режима | Llama-3.1-8B | Определяет тип задачи (feature/bug/refactor/question) |
| **Planner / Architect** | Создание пошагового плана (EN) | Qwen-32B | Обеспечение архитектурной целостности, атомарные шаги |
| **Memory Agent** | Долговременная память | ChromaDB | Поиск похожих прошлых решений как "Best Practice" |
| **Researcher Agent** | Сбор контекста | Через Context Manager | RAG (ChromaDB) + опционально Web Search |
| **Context Manager** | Динамическое управление контекстом | Tree-sitter, AST | Подача только релевантных сигнатур кода (<4000 токенов) |
| **TestGenerator** | Написание тестов (Red-стадия TDD) | Qwen-32B | Покрытие тестами ≥85%, описательные имена |
| **Coder** | Написание кода (Green-стадия TDD) | Qwen-32B | Минимально необходимый код для прохождения тестов |
| **Validator** | Проверка качества | Pytest, Mypy, Ruff | Запуск тестов, проверка типов, линтинг |
| **Debugger** | Анализ ошибок и Self-Healing | Qwen-32B | Анализ traceback, создание инструкций для Coder |
| **Reflection** | Финальная проверка качества | Qwen-32B / Llama-8B | Проверка SOLID/DRY/KISS, рефакторинг |

### Workflow агентов (реализован через LangGraph)

Граф определён в `infrastructure/workflow_graph.py`:

1. **Intent** → Определение задачи (RU → IntentResult с type, confidence)
2. **Planner** → Список атомарных шагов (EN, первый шаг — интерфейс/структура)
3. **Researcher** → Сбор контекста (RAG + Web, если нужно) + файл для modify/debug
4. **TestGenerator** → Создание тестов (Red-стадия, должны провалиться)
5. **Coder** → Реализация кода (Green-стадия, минимальный код для тестов)
6. **Validator** → Проверка (Pytest + Mypy + Bandit)
7. **Debugger** → Если ошибка, анализ и инструкции для Coder (Self-Healing, до max_iterations)
8. **Fixer** → Исправление кода по инструкциям от Debugger
9. **Reflection** → Финальная проверка на "запах кода" и соответствие `.cursor/rules`

Условные переходы:
- После Intent: если greeting → END, иначе → Planner
- После Validator: если all_passed или iteration >= max_iterations → Reflection, иначе → Debugger
- После Fixer: → Validator (цикл self-healing)

### Цикл Self-Healing

**Validator (Fail) → Debugger → Coder (Retry) → Validator (Pass) → Reflection**

Максимум 3 итерации для предотвращения зацикливания.

---

## 🛠️ Технологический стек

### Основные инструменты

| Категория | Инструмент | Назначение |
| :--- | :--- | :--- |
| **Оркестрация** | LangGraph | Управление графом агентов и состояний (workflow_graph.py) |
| **LLM** | Ollama | Локальный хостинг моделей |
| **RAG** | ChromaDB | Векторное хранилище для долговременной памяти |
| **Эмбеддинги** | nomic-embed-text | Генерация векторных представлений кода |
| **Тестирование** | Pytest | Unit-тесты, TDD |
| **Типизация** | Mypy (--strict) | Строгая проверка типов |
| **Линтинг** | Bandit | Проверка безопасности кода |
| **Backend** | FastAPI | REST API с SSE стримингом |
| **Frontend** | React + TypeScript + Vite | Веб-интерфейс |
| **Веб-поиск** | requests + beautifulsoup4 | Опциональный поиск в интернете |

### Бесплатные сервисы (опционально)

- **Tavily**: 1000 запросов/мес — AI-native поиск
- **Brave Search**: 2000 запросов/мес — альтернатива Google
- **SearXNG**: Полностью локальный мета-поисковик (Docker)
- **Langfuse**: Open-source мониторинг LangGraph (локально)

---

## 💻 Конфигурация для RTX 3090 (24GB VRAM)

### Рекомендуемый Model Zoo

| Роль Агента | Модель | Квантование | VRAM Usage | Почему |
| :--- | :--- | :--- | :--- | :--- |
| **Coder / Debugger** | `qwen2.5-coder:32b-instruct-q4_K_M` | Q4_K_M | ~19 GB | Топ-1 для кодинга, понимает сложные архитектуры |
| **Planner / Reflection** | `llama3.1:8b-instruct-q8_0` | Q8_0 | ~8 GB | Высокая точность логики и следования инструкциям |
| **Researcher / RAG** | `nomic-embed-text` | F16 | < 1 GB | Стандарт для эмбеддингов, очень быстрый |

**Важно**: Если запускаете Coder и Planner одновременно, 24GB может не хватить. В этом случае используйте `qwen2.5-coder:14b` — она оставляет больше места для контекста.

### Переменные окружения Ollama

```bash
# Flash Attention (ускоряет обработку длинного кода в 2-3 раза)
OLLAMA_FLASH_ATTENTION=1

# Держать в памяти Coder и Planner одновременно
OLLAMA_MAX_LOADED_MODELS=2

# Параллельные запросы (для многоагентных систем)
OLLAMA_NUM_PARALLEL=2

# Ускорение загрузки моделей с NVMe SSD
OLLAMA_NOPRUNE=1
```

### Оптимизация памяти

1. **KV Cache**: При 32B модели и контексте 32k токенов может занимать 2-4 GB. Ограничьте до `num_ctx: 16384` если система тормозит.
2. **Offloading**: Убедитесь, что `Llama.cpp: [GPU] 100% layers offloaded to GPU`. Если слои уходят на CPU, скорость падает в 10-20 раз.
3. **SSD Swap**: Файл подкачки на NVMe SSD не менее 16-32 GB для пиковых нагрузок на RAM.

---

## 🎯 Типы задач (Intent Types)

Система определяет тип задачи через Intent Agent:

| Тип | Описание | Workflow |
| :--- | :--- | :--- |
| **create** | Создать новый код/функцию/файл | Полный workflow: Intent → Planner → Researcher → TestGen → Coder → Validator → Reflection |
| **modify** | Изменить существующий код | Полный workflow + загрузка файла как контекст |
| **debug** | Найти и исправить ошибки | Полный workflow + загрузка файла + цикл self-healing |
| **optimize** | Оптимизировать код | Полный workflow с фокусом на производительность |
| **explain** | Объяснить как работает код | Упрощённый workflow (без генерации кода) |
| **test** | Написать тесты | Workflow до TestGenerator |
| **refactor** | Рефакторинг кода | Полный workflow с фокусом на структуру |
| **greeting** | Приветствие | Только Intent → END |

### Self-Healing цикл

Автоматическое исправление ошибок:
- Если Validator находит ошибки → Debugger анализирует
- Debugger генерирует инструкции → Fixer исправляет код
- Fixer → Validator (повторная проверка)
- Цикл повторяется до max_iterations или до успешной валидации

---

## 📝 Системные промпты (ключевые инструкции)

### TestGenerator Agent
> "Напиши **только** pytest-тесты, которые **должны провалиться** на текущем коде. Имена тестов должны быть максимально описательными. Используй моки для внешних зависимостей."

### Coder Agent
> "Напиши код, который заставит **все** предоставленные тесты пройти. Используй Type Hints везде и пиши docstrings на английском. Не добавляй лишнего кода, только то, что нужно для прохождения тестов."

### Reflection Agent
> "Проверь код на соответствие **SOLID, DRY, KISS** и правилам из `.cursor/rules/`. Если найдены проблемы, опиши их (RU) и предложи **конкретные исправления** в формате патча."

### Debugger Agent
> "Проанализируй Traceback и код. Объясни причину ошибки (RU). Сформулируй **конкретную, атомарную инструкцию** для Coder-агента, как исправить ошибку."

### Context Manager Agent
> "Собери контекст для LLM. Используй AST для определения зависимостей. Включи **только сигнатуры функций и docstrings**. Общий контекст не должен превышать 4000 токенов."

### Memory Agent
> "Используй ChromaDB для поиска прошлых решений. Если найдено релевантное решение, предоставь его Planner-у как **'Best Practice Example'**."

---

## 📚 Правила проекта (`.cursor/rules/`)

Эти файлы служат **контекстом с наивысшим приоритетом** для всех агентов.

### core_vision.md
- 100% локальность, русский CLI, TDD
- Покрытие тестами ≥85%
- Отсутствие "запаха кода": SOLID, DRY, KISS

### agent_workflow.md
- Последовательность: Intent → Planner → Researcher → TestGenerator → Coder → Validator → Reflection
- Цикл Self-Healing с максимум 3 итерациями

### quality_metrics.md
- `mypy --strict` обязателен
- `ruff` для линтинга
- Docstrings в стиле Google на английском
- Обработка исключений: никаких пустых `except: pass`
- Логирование всех действий агентов в `utils/logger.py`

### local_models.md
- Код: `qwen2.5-coder:32b` или `deepseek-coder`
- Логика/Планирование: `llama3.1:8b` или `mistral`
- Эмбеддинги: `nomic-embed-text`
- Параметры: `temperature=0` для кода, `temperature=0.2` для планирования

---

## 🔧 Структура проекта

```
cursor-killer/
├── agents/              # Реализация агентов
│   ├── intent.py
│   ├── planner.py
│   ├── researcher.py
│   ├── test_generator.py
│   ├── coder.py
│   ├── debugger.py
│   ├── reflection.py
│   └── memory.py
├── infrastructure/      # Инфраструктура
│   ├── local_llm.py    # Обертка над Ollama
│   ├── rag.py          # Интеграция с ChromaDB
│   ├── web_search.py   # Веб-поиск
│   ├── workflow_state.py  # State схема для LangGraph
│   ├── workflow_nodes.py  # Узлы графа
│   ├── workflow_edges.py  # Условные переходы
│   └── workflow_graph.py  # Граф LangGraph
├── backend/            # FastAPI backend
│   ├── api.py         # Главное приложение
│   └── routers/       # API роутеры
├── frontend/          # React + TypeScript + Vite
├── tests/              # Тесты проекта
├── utils/              # Утилиты
│   └── logger.py
├── .cursor/rules/      # Правила для агентов
│   ├── core_vision.md
│   ├── agent_workflow.md
│   ├── quality_metrics.md
│   └── local_models.md
├── cli.py              # Точка входа CLI
└── config.toml         # Конфигурация
```

---

## 🎨 Инженерные хитрости

### 1. Маршрутизация задач (Router Pattern)
Используйте легкие модели (Llama-8B) для простых задач (классификация, извлечение JSON), тяжелые (Qwen-32B) — только для сложных (кодинг, рефакторинг).

### 2. Кэширование контекста (Prompt Caching)
Используйте `llama.cpp` или `vLLM` в качестве бэкенда Ollama для кэширования промптов — ускоряет повторные запросы на 80-90%.

### 3. "Скелетный" RAG
Индексируйте в ChromaDB только сигнатуры функций и docstrings, а не весь код. Это ускоряет поиск и не перегружает память.

### 4. Async Everything
Используйте `asyncio` в Python — агенты могут ждать ответа от Ollama и одновременно выполнять поиск в интернете.

---

## 🚀 Процесс реализации

### Шаг 1: Подготовка окружения

```bash
mkdir cursor-killer && cd cursor-killer
python -m venv .venv
source .venv/bin/activate

pip install langchain langgraph ollama chromadb pytest mypy ruff \
    requests beautifulsoup4 pydantic textual tree-sitter

# Загрузка моделей
ollama pull qwen2.5-coder:32b
ollama pull llama3.1:8b
ollama pull nomic-embed-text
```

### Шаг 2: Создание структуры

```bash
mkdir -p agents infrastructure tests utils .cursor/rules configs/modes
```

### Шаг 3: Реализация по порядку

1. **Инфраструктура**: `infrastructure/local_llm.py`, `infrastructure/rag.py`
2. **Базовые агенты**: `Intent`, `Planner`, `Researcher`
3. **TDD-агенты**: `TestGenerator`, `Coder`, `Validator`
4. **Продвинутые**: `Debugger`, `Reflection`, `Context Manager`, `Memory`
5. **Интеграция**: LangGraph workflow, CLI, TUI

### Шаг 4: Тестирование

End-to-End тест: проверка всего потока `Intent → Planner → Researcher → TestGenerator → Coder → Validator → Reflection`.

---

## 📊 Метрики качества

| Метрика | Требование |
| :--- | :--- |
| **Покрытие тестами** | ≥85% |
| **Self-Healing Limit** | Максимум 3 итерации Debugger-Coder-Validator |
| **Context Efficiency** | Коэффициент использования контекста ≥ 0.8 |
| **Memory Usage** | Все успешные архитектурные решения сохраняются в Long-Term Memory |
| **Type Safety** | `mypy --strict` без ошибок |
| **Code Style** | `ruff` без предупреждений |

---

## 🎯 Итоговое видение

**"Cursor Killer Pro"** — это не просто кодогенератор, а полноценная система автоматической разработки с:

- **Автоматическим качеством**: Reflection Agent гарантирует отсутствие "запаха кода"
- **Самообучением**: Memory Agent сохраняет лучшие практики
- **Самоисцелением**: Debugger Agent исправляет ошибки автоматически
- **Адаптивностью**: Режимы работы оптимизируют процесс под задачу
- **Локальностью**: 100% оффлайн, полная приватность

Система предназначена для разработчиков, которые ценят качество кода, следуют TDD и хотят иметь полный контроль над процессом разработки без зависимости от облачных сервисов.

---

*Документ создан на основе анализа всех файлов проекта: архитектурных спецификаций, гайдов по реализации, промптов агентов, конфигураций и исследовательских заметок.*