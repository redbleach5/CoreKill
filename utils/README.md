# 🛠️ Утилиты проекта

**Обновлено:** 2026-01-23  
**Всего утилит:** 13

---

## 📋 Содержание

- [Критичные утилиты](#критичные-утилиты)
- [Утилиты для работы с данными](#утилиты-для-работы-с-данными)
- [Утилиты для работы с файлами](#утилиты-для-работы-с-файлами)
- [Утилиты для моделей и LLM](#утилиты-для-моделей-и-llm)
- [Утилиты для валидации](#утилиты-для-валидации)
- [Утилиты для UI и конфигурации](#утилиты-для-ui-и-конфигурации)
- [CLI утилиты](#cli-утилиты)

---

## 🔴 Критичные утилиты

Эти утилиты используются везде в проекте и критичны для работы системы.

### `logger.py` ⭐
**Назначение:** Система логирования с поддержкой новой инфраструктуры логирования

**Основные функции:**
- `setup_logger()` — настройка логгера
- `get_logger()` — получение глобального логгера
- `get_log_manager()` — получение LogManager для прямой работы

**Использование:**
```python
from utils.logger import get_logger

logger = get_logger()
logger.info("Сообщение")
logger.error("Ошибка", error=exception)
```

**Зависимости:** `infrastructure.logging`

---

### `config.py` ⭐
**Назначение:** Загрузка и работа с конфигурацией из `config.toml`

**Основные классы:**
- `Config` — singleton для доступа к конфигурации

**Использование:**
```python
from utils.config import get_config

config = get_config()
model = config.default_model
temperature = config.temperature
```

**Зависимости:** `tomllib` / `tomli`, `utils.logger`

---

### `model_checker.py` ⭐
**Назначение:** Проверка доступности моделей Ollama, анализ размеров, выбор оптимальной модели

**Основные функции:**
- `check_ollama_api_available()` — проверка доступности Ollama API
- `check_model_available(model_name)` — проверка доступности модели
- `scan_available_models()` — сканирование всех доступных моделей
- `get_light_model()` — получение легкой модели
- `get_coder_model()` — получение модели для кода
- `get_reasoning_model()` — получение reasoning модели
- `get_best_model_for_complexity()` — выбор модели по сложности задачи

**Классы:**
- `TaskComplexity` — enum сложности задачи (SIMPLE, MEDIUM, COMPLEX)
- `ModelInfo` — информация о модели (размер, параметры, качество)

**Использование:**
```python
from utils.model_checker import get_coder_model, scan_available_models

# Получить лучшую модель для кода
model = get_coder_model(min_quality=0.7)

# Сканировать все доступные модели
models = scan_available_models()
for name, info in models.items():
    print(f"{name}: {info.estimated_quality}")
```

**Зависимости:** `ollama`, `re`, `dataclasses`, `enum`, `utils.logger`

---

### `path_validator.py` ⭐
**Назначение:** Валидация и ограничение путей к файлам (защита от path traversal атак)

**Основные функции:**
- `get_project_root()` — получение корня проекта
- `validate_file_path()` — валидация пути к файлу
- `validate_directory_path()` — валидация пути к директории

**Использование:**
```python
from utils.path_validator import validate_file_path

# Валидация пути (выбросит HTTPException если путь вне проекта)
safe_path = validate_file_path("path/to/file.py", project_path="/project/root")
```

**Зависимости:** `pathlib`, `fastapi`, `utils.logger`

---

## 💾 Утилиты для работы с данными

### `db_cli.py` ✅
**Назначение:** CLI для управления базами данных системы (ChromaDB, JSON)

**Команды:**
- `list` — список всех БД
- `stats` — статистика по БД
- `backup` — создание бэкапа
- `restore` — восстановление из бэкапа
- `cleanup` — очистка старых данных

**Использование:**
```bash
python -m utils.db_cli list
python -m utils.db_cli backup --all
python -m utils.db_cli cleanup json:conversations --days 30 --execute
```

**Документация:** Подробная документация в основном [README.md](../../README.md#управление-базами-данных)

**Зависимости:** `infrastructure.database_manager`, `utils.logger`

---

### `artifact_saver.py`
**Назначение:** Автоматическое сохранение артефактов задачи (код, тесты, рефлексия, метрики)

**Основные методы:**
- `create_task_directory(task)` — создание директории для задачи
- `save_code(code, filename)` — сохранение кода
- `save_tests(tests, filename)` — сохранение тестов
- `save_reflection(reflection_data, filename)` — сохранение рефлексии (Markdown)
- `save_metrics(metrics, filename)` — сохранение метрик (JSON)
- `save_all_artifacts()` — сохранение всех артефактов

**Использование:**
```python
from utils.artifact_saver import ArtifactSaver

saver = ArtifactSaver()
saver.create_task_directory("Создать калькулятор")
saver.save_code(code, "calculator.py")
saver.save_tests(tests, "test_calculator.py")
```

**Зависимости:** `pathlib`, `json`, `datetime`

---

### `token_counter.py`
**Назначение:** Приблизительный подсчет токенов в тексте

**Основные функции:**
- `estimate_tokens(text)` — оценка количества токенов в тексте
- `estimate_workflow_tokens()` — оценка токенов в workflow
- `check_token_limit()` — проверка превышения лимитов токенов

**Использование:**
```python
from utils.token_counter import estimate_tokens, check_token_limit

tokens = estimate_tokens("Привет, мир!")
result = check_token_limit(tokens, warning_threshold=30000, max_tokens=50000)
```

**Зависимости:** `typing`

---

## 📁 Утилиты для работы с файлами

### `file_context.py`
**Назначение:** Работа с файлами при режимах modify/debug

**Основные функции:**
- `extract_file_path_from_task(task)` — извлечение пути к файлу из задачи
- `read_file_context(file_path, max_lines)` — чтение содержимого файла для контекста
- `prepare_modify_context(task, existing_file_content)` — подготовка контекста для режима modify/debug

**Использование:**
```python
from utils.file_context import extract_file_path_from_task, read_file_context

# Извлечь путь к файлу из задачи
file_path = extract_file_path_from_task("Исправить ошибку в файле: src/main.py")

# Прочитать контекст файла
context = read_file_context(file_path, max_lines=1000)
```

**Зависимости:** `pathlib`

---

## 🤖 Утилиты для моделей и LLM

### `intent_helpers.py`
**Назначение:** Утилиты для работы с типами intent (намерений). Устраняет дублирование словарей intent_descriptions в разных модулях.

**Основные функции:**
- `get_intent_description(intent_type, format)` — получение описания типа intent (short/long/planning)
- `get_all_intent_types()` — получение всех доступных типов intent

**Использование:**
```python
from utils.intent_helpers import get_intent_description, get_all_intent_types

# Получить описание типа intent
short_desc = get_intent_description("create", "short")
long_desc = get_intent_description("create", "long")

# Получить все доступные типы
all_types = get_all_intent_types()
```

**Зависимости:** `typing`

**Связанные модули:** `agents.intent` — использует для описаний типов намерений

---

### `structured_helpers.py`
**Назначение:** Утилиты для работы со structured output. Предоставляет единую точку входа для generate_structured с fallback и feature flag проверку.

**Основные функции:**
- `is_structured_output_enabled(agent_name)` — проверка включён ли structured output для агента
- `generate_with_fallback()` — синхронная генерация с fallback на legacy метод
- `generate_with_fallback_async()` — асинхронная генерация с fallback

**Использование:**
```python
from utils.structured_helpers import generate_with_fallback, is_structured_output_enabled
from models.agent_responses import IntentResponse

# Проверка включён ли structured output
if is_structured_output_enabled("intent"):
    # Синхронная версия
    response = generate_with_fallback(
        llm=self.llm,
        prompt=prompt,
        response_model=IntentResponse,
        fallback_fn=lambda: self._classify_legacy(query),
        agent_name="intent"
    )
```

**Зависимости:** `pydantic`, `infrastructure.local_llm`, `utils.logger`, `utils.config`

**Связанные модули:** `agents.intent`, `agents.debugger`, `agents.reflection` — используют для structured output

---

## ✅ Утилиты для валидации

### `validation.py`
**Назначение:** Утилиты для валидации кода: pytest, mypy, bandit. Предоставляет функции для проверки сгенерированного кода.

**Основные функции:**
- `check_syntax(code_str)` — быстрая синтаксическая проверка (ast.parse)
- `check_syntax_both(code_str, test_str)` — проверка синтаксиса кода и тестов
- `run_pytest(code_str, test_str)` — запуск pytest для тестов
- `check_mypy(code_str)` — запуск mypy для проверки типов
- `check_bandit(code_str)` — запуск bandit для проверки безопасности
- `validate_code(code_str, test_str)` — полная валидация (pytest + mypy + bandit)
- `validate_code_quick(code_str, test_str)` — быстрая валидация (только синтаксис + pytest)

**Использование:**
```python
from utils.validation import validate_code, check_syntax, run_pytest

# Быстрая проверка синтаксиса
success, message = check_syntax(code)
if not success:
    print(f"Синтаксическая ошибка: {message}")

# Запуск pytest
result = run_pytest(code, tests)
if result["success"]:
    print("Все тесты прошли!")

# Полная валидация
results = validate_code(code, tests)
print(f"pytest: {results['pytest']['success']}")
print(f"mypy: {results['mypy']['success']}")
print(f"bandit: {results['bandit']['success']}")
```

**Зависимости:** `subprocess`, `tempfile`, `ast`, `pathlib`, `utils.logger`

**Связанные модули:** `agents.validator`, `infrastructure.workflow_nodes` — используют для валидации кода

**Безопасность:** Использует `subprocess.run()` с изоляцией процесса вместо `exec()` для безопасного выполнения кода

---

## 🎨 Утилиты для UI и конфигурации

### `ui_delays.py`
**Назначение:** Управление задержками UI для плавности интерфейса

**Основные функции:**
- `get_ui_delay()` — получение задержки для UI событий
- `get_critical_delay()` — получение задержки для критических событий
- `ui_sleep(delay_type)` — выполнение задержки

**Использование:**
```python
from utils.ui_delays import ui_sleep

# Задержка для обычных событий
await ui_sleep("normal")

# Задержка для критических событий
await ui_sleep("critical")
```

**Зависимости:** `asyncio`, `utils.config`

---

### `env_config.py`
**Назначение:** Управление конфигурацией из переменных окружения. Предоставляет Pydantic модель для работы с переменными окружения.

**Основные функции:**
- `get_env_config()` — получение конфигурации из переменных окружения (кэшируется)

**Классы:**
- `EnvironmentConfig` — Pydantic модель для конфигурации из env vars

**Переменные окружения:**
- `ENVIRONMENT` — окружение (development/production)
- `DEBUG` — режим отладки
- `BACKEND_PORT` — порт backend
- `FRONTEND_PORT` — порт frontend
- `OLLAMA_BASE_URL` — URL Ollama API
- `DEFAULT_MODEL` — модель по умолчанию
- `TEMPERATURE` — температура для LLM
- И многие другие (см. `EnvironmentConfig`)

**Использование:**
```python
from utils.env_config import get_env_config

# Получить конфигурацию из переменных окружения
env_config = get_env_config()

# Доступ к настройкам
model = env_config.default_model
temperature = env_config.temperature
ollama_url = env_config.ollama_base_url

# Проверка окружения
if env_config.is_production():
    print("Запущено в production")
```

**Зависимости:** `pydantic`, `os`, `functools`

**Связанные модули:** `utils.config` — основная конфигурация из config.toml

**Примечания:**
- Конфигурация кэшируется (lru_cache)
- Переменные окружения имеют приоритет над config.toml
- Все значения валидируются через Pydantic

---

## 📊 Статистика

- **Всего утилит:** 13
- **Критичные:** 4 (logger, config, model_checker, path_validator)
- **CLI:** 1 (db_cli)
- **Задокументированы:** 13 (все утилиты имеют docstrings с примерами)
- **Статус документации:** ✅ Полностью задокументировано

---

## 🔗 Связанные документы

- [Каталог всех инструментов](../../docs/TOOLS_CATALOG.md)
- [План организации](../../docs/archive/ORGANIZATION_PLAN_SIMPLE.md) (в архиве)
- [Основной README](../../README.md)

---

**Последнее обновление:** 2026-01-23
