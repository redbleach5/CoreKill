# Скрипты проекта

**Обновлено:** 2026-01-23

> **Тестовые документы** перемещены в `docs/archive/scripts/`

---

## 📋 Содержание

- [Скрипты анализа и проверки](#скрипты-анализа-и-проверки)
- [Тестовые скрипты](#тестовые-скрипты)
- [Shell скрипты](#shell-скрипты)

---

## 🔍 Скрипты анализа и проверки

### analyze_dependencies.py

Скрипт для статического анализа импортов и построения графа зависимостей модулей.

### Возможности

1. **Автоматическое извлечение импортов** из всех `.py` файлов проекта
2. **Построение направленного графа** зависимостей
3. **Анализ на:**
   - Циклические зависимости (недопустимые петли)
   - Сиротские модули (никем не используемые)
   - Нарушение слоёв архитектуры
   - Отсутствующие/недоступные зависимости

### Использование

```bash
# Запуск анализа
python3 scripts/analyze_dependencies.py

# Результаты сохраняются в:
# - dependency_analysis.json (JSON с результатами)
# - dependency_graph.png (визуализация графа, если установлены networkx и matplotlib)
```

### Установка зависимостей

```bash
# Для визуализации графа
pip install networkx matplotlib

# Или через requirements
pip-compile --upgrade --output-file=requirements.txt requirements.in
pip install -r requirements.txt
```

### Архитектурные слои

Скрипт проверяет следующие правила импорта:

- **frontend/** - UI слой (не должен импортировать backend/agents/infrastructure)
- **backend/** - API слой (может импортировать agents, infrastructure, utils, models)
- **agents/** - Бизнес-логика (может импортировать infrastructure, utils, models)
- **infrastructure/** - Инфраструктура (может импортировать utils, models)
- **utils/** - Утилиты (базовый слой, не должен импортировать другие слои)
- **models/** - Модели данных (базовый слой, не должен импортировать другие слои)
- **tests/** - Тесты (могут импортировать всё)

### Интерпретация результатов

#### Циклические зависимости
❌ **Критично** - указывает на архитектурные проблемы. Модули не должны зависеть друг от друга циклически.

#### Сиротские модули
⚠️ **Информационно** - модули, которые не импортируются напрямую. Это нормально, если они используются через:
- Dependency Injection (DependencyContainer)
- Динамические импорты
- Точки входа (main.py, cli.py)

#### Нарушения слоёв
❌ **Критично** - указывает на нарушение архитектурных правил. Например, `utils` не должен импортировать `agents`.

#### Отсутствующие зависимости
⚠️ **Предупреждение** - модули, которые импортируются, но не существуют. Может указывать на:
- Опечатки в импортах
- Удалённые модули
- Неправильные пути

### Пример вывода

```
🔍 Поиск Python файлов...
✅ Найдено 125 Python файлов

📊 Построение графа зависимостей...
✅ Граф построен: 107 модулей, 179 зависимостей

🔄 Поиск циклических зависимостей...
✅ Циклических зависимостей не найдено

👻 Поиск сиротских модулей...
⚠️ Найдено 88 сиротских модулей:
  - agents/chat.py
  - agents/coder.py
  ...

🚫 Проверка нарушений слоёв архитектуры...
✅ Нарушений слоёв архитектуры не найдено

❓ Проверка отсутствующих зависимостей...
✅ Отсутствующих зависимостей не найдено
```

---

### check_imports.py ✅

**Назначение:** Проверка импортов в frontend компонентах

**Проверяет:**
- Использование `api` без импорта
- Использование `useLocalStorage` без импорта
- Использование `useModels` без импорта
- Использование `useApi` без импорта

**Использование:**
```bash
python3 scripts/check_imports.py
```

**Документация:** Упоминается в `docs/TESTING.md`

---

### check_undefined.py ✅

**Назначение:** Комплексная проверка неопределенных переменных и констант в frontend

**Проверяет:**
- Использование переменных/констант без определения
- Отсутствующие импорты
- Использование констант до их определения

**Использование:**
```bash
python3 scripts/check_undefined.py
```

**Документация:** Упоминается в `docs/TESTING.md`

---

### check_refactoring_imports.py

**Назначение:** Проверка корректности импортов после рефакторинга

**Проверяет:**
1. Все новые модули импортируются без ошибок
2. Нет циклических зависимостей
3. Все используемые импорты существуют

**Использование:**
```bash
python3 scripts/check_refactoring_imports.py
```

**Зависимости:** `importlib`, `sys`, `pathlib`

---

### check_imports.js

**Назначение:** Проверка импортов (Node.js версия, возможно дубликат `check_imports.py`)

**Использование:**
```bash
node scripts/check_imports.js
```

**Примечание:** Возможно дубликат Python версии, требует проверки необходимости

---

## 🧪 Тестовые скрипты

### test_thinking_streaming.py

**Назначение:** Тесты thinking стриминга (reasoning моделей)

**Использование:**
```bash
python3 scripts/test_thinking_streaming.py
```

**Зависимости:** (требуется проверка)

---

### test_thinking_integration.py

**Назначение:** Интеграционные тесты thinking стриминга

**Использование:**
```bash
python3 scripts/test_thinking_integration.py
```

**Зависимости:** (требуется проверка)

---

### test_autonomous_improver_logic.py

**Назначение:** Тесты логики Autonomous Improver

**Использование:**
```bash
python3 scripts/test_autonomous_improver_logic.py
```

**Зависимости:** (требуется проверка)

---

## 🐚 Shell скрипты

### start_improver_test.sh

**Назначение:** Запуск тестов Autonomous Improver

**Использование:**
```bash
./scripts/start_improver_test.sh
```

---

### RUN_TEST.sh

**Назначение:** Запуск тестов (общий скрипт)

**Использование:**
```bash
./scripts/RUN_TEST.sh
```

---

## 📊 Статистика

- **Всего скриптов:** 9
- **Задокументированы:** 2 (analyze_dependencies, check_imports, check_undefined)
- **Требуют документации:** 6

---

## 🔗 Связанные документы

- [Каталог всех инструментов](../docs/TOOLS_CATALOG.md)
- [План организации](../docs/archive/ORGANIZATION_PLAN_SIMPLE.md) (в архиве)
- [Тестирование](../docs/TESTING.md)
