# Тест логики Autonomous Improver

## Описание

Скрипт `test_autonomous_improver_logic.py` проверяет корректность работы всех компонентов Autonomous Improver:

1. ✅ Определение типа проекта (язык, домен, фреймворк)
2. ✅ Выбор адаптера (PythonAdapter, FrontendAdapter, MultiAdapter)
3. ✅ Обнаружение файлов (все поддерживаемые расширения)
4. ✅ Фильтрация файлов (исключённые директории и паттерны)
5. ✅ Приоритизация файлов (по сложности, размеру, docstrings)
6. ✅ Анализ структуры файлов (AST для Python, regex для Frontend)
7. ✅ Построение промптов (языковые и доменные правила)
8. ✅ Запуск простой задачи анализа (максимум 5 файлов)

## Использование

### Базовый запуск:

```bash
python3 scripts/test_autonomous_improver_logic.py
```

### Или с правами на выполнение:

```bash
./scripts/test_autonomous_improver_logic.py
```

## Что проверяет скрипт

### 1. Определение типа проекта
- Проверяет, что проект правильно определяется как `python`, `typescript`, `javascript` или `mixed`
- Проверяет определение домена (`backend`, `frontend`, `fullstack`)
- Проверяет определение фреймворка (`fastapi`, `react`, `vue`, и т.д.)

### 2. Выбор адаптера
- Проверяет, что выбран правильный адаптер на основе типа проекта
- Проверяет поддерживаемые расширения файлов

### 3. Обнаружение файлов
- Проверяет, что все файлы правильных типов найдены
- Показывает распределение по расширениям

### 4. Фильтрация файлов
- Проверяет, что исключённые директории действительно исключены
- Проверяет, что исключённые паттерны работают

### 5. Приоритизация файлов
- Проверяет, что файлы правильно сортируются по приоритету
- Показывает топ-10 приоритетных файлов

### 6. Анализ структуры
- Проверяет, что структура файлов правильно анализируется
- Показывает контекст для первых 3 файлов

### 7. Построение промптов
- Проверяет, что промпты правильно строятся
- Проверяет наличие всех необходимых элементов

### 8. Простая задача анализа
- Запускает реальный анализ (максимум 5 файлов)
- Показывает результаты и предложения

## Пример вывода

```
================================================================================
  ТЕСТ ЛОГИКИ AUTONOMOUS IMPROVER
================================================================================

🔧 Инициализация Autonomous Improver...
✅ Проект: /Users/ruslan/Downloads/testmatuscrsr
✅ Модель: qwen2.5-coder:1.5b
✅ Мин. уверенность: 0.9

================================================================================
  1. Определение типа проекта
================================================================================
✅ Язык проекта: mixed
✅ Домен: fullstack
✅ Фреймворк: fastapi
✅ Найдено .py файлов: 150
✅ Найдено .ts/.tsx файлов: 35
✅ Найдено .js/.jsx файлов: 0
✅ Определение языка: КОРРЕКТНО

================================================================================
  2. Выбор адаптера
================================================================================
✅ Выбранный адаптер: mixed
✅ Поддерживаемые расширения: ['.py', '.ts', '.tsx', '.js', '.jsx', '.html', '.md', '.json']
✅ Выбор адаптера: КОРРЕКТНО

...
```

## Интерпретация результатов

- ✅ **Все проверки пройдены** - логика работает корректно
- ⚠️ **Некоторые проверки не пройдены** - есть проблемы, нужно исправить
- ❌ **Критическая ошибка** - скрипт не смог запуститься

## Устранение проблем

### Проблема: Неправильное определение типа проекта

**Решение:**
- Проверить наличие файлов `.py`, `.ts`, `.js` в проекте
- Проверить логику в `ProjectProfile.detect_from_project()`

### Проблема: Неправильный выбор адаптера

**Решение:**
- Проверить логику в `AutonomousImprover.__init__()`
- Убедиться, что `profile.language` правильно определён

### Проблема: Файлы не найдены

**Решение:**
- Проверить, что `project_path` правильный
- Проверить, что адаптер поддерживает нужные расширения

### Проблема: Исключённые файлы не исключаются

**Решение:**
- Проверить `excluded_directories` в `ProjectProfile`
- Убедиться, что `.mypy_cache` и другие кэш-директории добавлены

## Дополнительная информация

Скрипт использует реальный экземпляр `AutonomousImprover`, но:
- Не запускает фоновую задачу (только синхронный анализ)
- Ограничивает количество файлов для быстрого теста
- Не изменяет состояние проекта

Для полного теста используйте:
```bash
./run_improver.sh 4.0  # 4 часа теста
```
