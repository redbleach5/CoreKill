# 🐚 Shell скрипты проекта

**Обновлено:** 2026-01-23

---

## 📋 Содержание

- [Скрипты в корне проекта](#скрипты-в-корне-проекта)
- [Скрипты в scripts/](#скрипты-в-scripts)

---

## 📁 Скрипты в корне проекта

### `run_improver.sh` ⭐

**Расположение:** `run_improver.sh` (корень проекта)  
**Назначение:** Простой скрипт для запуска Autonomous Improver

**Использование:**
```bash
# Запуск с длительностью по умолчанию (4 часа)
./run_improver.sh

# Запуск с указанной длительностью
./run_improver.sh 4.0
./run_improver.sh 8.0
```

**Что делает:**
1. Проверяет, что `autonomous_improver.enabled = true` в `config.toml`
2. Создаёт директорию для логов (`logs/`)
3. Запускает тест через `infrastructure/autonomous_improver/scripts/test.py`
4. Выводит информацию о результатах

**Результаты:**
- Логи модуля: `logs/autonomous_improver.log`
- Логи теста: `logs/autonomous_improver_test.log`
- Результаты: `test_improver_results.json`

**Примеры:**
```bash
# Просмотр логов в реальном времени
tail -f logs/autonomous_improver.log

# Анализ результатов
python3 infrastructure/autonomous_improver/scripts/analyze_results.py test_improver_results.json
```

**Зависимости:**
- Python 3
- `infrastructure/autonomous_improver/scripts/test.py`
- `config.toml` с `autonomous_improver.enabled = true`

---

### `setup_models.sh`

**Расположение:** `setup_models.sh` (корень проекта)  
**Назначение:** Установка моделей Ollama

**Использование:**
```bash
./setup_models.sh
```

**Что делает:**
- Устанавливает необходимые модели Ollama для проекта
- Проверяет доступность Ollama
- Выводит список установленных моделей

**Зависимости:**
- Ollama должен быть установлен и запущен

---

## 📜 Скрипты в scripts/

### `start_improver_test.sh`

**Расположение:** `scripts/start_improver_test.sh`  
**Назначение:** Удобный скрипт для запуска теста Autonomous Improver с проверками

**Использование:**
```bash
# Запуск с длительностью по умолчанию (4 часа)
./scripts/start_improver_test.sh

# Запуск с указанной длительностью
./scripts/start_improver_test.sh 4.0
```

**Что делает:**
1. Проверяет конфигурацию (`autonomous_improver.enabled`)
2. Предлагает временно включить, если отключено
3. Проверяет наличие Ollama и моделей
4. Запускает тест через `scripts/test_autonomous_improver.py`
5. Корректно обрабатывает сигналы (Ctrl+C)
6. Восстанавливает конфигурацию при необходимости

**Особенности:**
- Интерактивный: спрашивает разрешение перед изменениями
- Безопасный: восстанавливает конфигурацию после теста
- Умный: проверяет наличие моделей перед запуском

**Зависимости:**
- Python 3
- Ollama
- `scripts/test_autonomous_improver.py`

---

### `RUN_TEST.sh`

**Расположение:** `scripts/RUN_TEST.sh`  
**Назначение:** Общий скрипт для запуска теста Autonomous Improver

**Использование:**
```bash
# Запуск с длительностью по умолчанию (4 часа)
./scripts/RUN_TEST.sh

# Запуск с указанной длительностью
./scripts/RUN_TEST.sh 4.0
```

**Что делает:**
1. Проверяет конфигурацию
2. Проверяет наличие Ollama и моделей
3. Запускает тест через `scripts/test_autonomous_improver.py`
4. Выводит информацию для анализа результатов

**Отличия от `start_improver_test.sh`:**
- Более простой (без интерактивности)
- Не изменяет конфигурацию автоматически
- Не восстанавливает конфигурацию

**Зависимости:**
- Python 3
- Ollama
- `scripts/test_autonomous_improver.py`

---

## 📊 Сравнение скриптов

| Скрипт | Расположение | Интерактивность | Восстановление конфигурации | Проверка моделей |
|--------|--------------|-----------------|----------------------------|------------------|
| `run_improver.sh` | корень | ❌ | ❌ | ❌ |
| `start_improver_test.sh` | scripts/ | ✅ | ✅ | ✅ |
| `RUN_TEST.sh` | scripts/ | ⚠️ (частично) | ❌ | ✅ |

---

## 💡 Рекомендации

### Для быстрого запуска:
```bash
./run_improver.sh 4.0
```

### Для безопасного тестирования:
```bash
./scripts/start_improver_test.sh 4.0
```

### Для простого запуска:
```bash
./scripts/RUN_TEST.sh 4.0
```

---

## 🔗 Связанные документы

- [Autonomous Improver README](../infrastructure/autonomous_improver/README.md)
- [Каталог всех инструментов](../docs/TOOLS_CATALOG.md)
- [План организации](../docs/archive/ORGANIZATION_PLAN_SIMPLE.md) (в архиве)

---

**Последнее обновление:** 2026-01-23
